\documentclass[lettersize,journal]{IEEEtran}

\usepackage{amsmath,amsfonts,amssymb}
\usepackage{bm}

\usepackage{algorithmic}
\usepackage{algorithm}

\usepackage{array}
\usepackage{booktabs}

\usepackage[caption=false,font=normalsize,
labelfont=sf,textfont=sf]{subfig}

\usepackage{textcomp}
\usepackage{stfloats}

\usepackage{url}
\usepackage{verbatim}

\usepackage{graphicx}
\usepackage{adjustbox}

\usepackage{cite}
\usepackage{balance}

\graphicspath{{fig3/}}

\hyphenation{
re-con-fig-ur-able
po-lar-i-met-ric
sub-car-ri-er
syn-chro-ni-za-tion
Cra-mer-Rao
}

\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{theorem}{Theorem}
\newtheorem{corollary}{Corollary}
\newtheorem{assumption}{Assumption}
\newtheorem{remark}{Remark}

% ---------------- shorthand ----------------
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\bp}{\mathbf{p}}
\newcommand{\bx}{\mathbf{x}}
\newcommand{\by}{\mathbf{y}}
\newcommand{\ba}{\mathbf{a}}
\newcommand{\bb}{\mathbf{b}}
\newcommand{\bc}{\mathbf{c}}
\newcommand{\bd}{\mathbf{d}}
\newcommand{\be}{\mathbf{e}}
\newcommand{\bh}{\mathbf{h}}
\newcommand{\bg}{\mathbf{g}}
\newcommand{\bv}{\mathbf{v}}
\newcommand{\bw}{\mathbf{w}}
\newcommand{\bn}{\mathbf{n}}
\newcommand{\br}{\mathbf{r}}
\newcommand{\bs}{\mathbf{s}}
\newcommand{\bt}{\mathbf{t}}
\newcommand{\bq}{\mathbf{q}}
\newcommand{\bz}{\mathbf{z}}
\newcommand{\bu}{\mathbf{u}}
\newcommand{\bA}{\mathbf{A}}
\newcommand{\bBm}{\mathbf{B}}
\newcommand{\bC}{\mathbf{C}}
\newcommand{\bD}{\mathbf{D}}
\newcommand{\bE}{\mathbf{E}}
\newcommand{\bF}{\mathbf{F}}
\newcommand{\bG}{\mathbf{G}}
\newcommand{\bH}{\mathbf{H}}
\newcommand{\bI}{\mathbf{I}}
\newcommand{\bJ}{\mathbf{J}}
\newcommand{\bK}{\mathbf{K}}
\newcommand{\bL}{\mathbf{L}}
\newcommand{\bM}{\mathbf{M}}
\newcommand{\bP}{\mathbf{P}}
\newcommand{\bQ}{\mathbf{Q}}
\newcommand{\bR}{\mathbf{R}}
\newcommand{\bS}{\mathbf{S}}
\newcommand{\bT}{\mathbf{T}}
\newcommand{\bU}{\mathbf{U}}
\newcommand{\bV}{\mathbf{V}}
\newcommand{\bX}{\mathbf{X}}
\newcommand{\bY}{\mathbf{Y}}
\newcommand{\bN}{\mathbf{N}}
\newcommand{\bW}{\mathbf{W}}
\newcommand{\bZ}{\mathbf{Z}}
\newcommand{\bTheta}{\boldsymbol{\Theta}}
\newcommand{\bPsi}{\boldsymbol{\Psi}}
\newcommand{\bOmega}{\boldsymbol{\Omega}}
\newcommand{\bSigma}{\boldsymbol{\Sigma}}
\newcommand{\bPi}{\boldsymbol{\Pi}}
\newcommand{\brho}{\boldsymbol{\rho}}
\newcommand{\bmu}{\boldsymbol{\mu}}
\newcommand{\bchi}{\boldsymbol{\chi}}
\newcommand{\bxi}{\boldsymbol{\xi}}
\newcommand{\bvt}{\boldsymbol{\vartheta}}
\newcommand{\balpha}{\boldsymbol{\alpha}}
\newcommand{\Real}{\operatorname{Re}}
\newcommand{\Imag}{\operatorname{Im}}
\newcommand{\rank}{\operatorname{rank}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\diag}{\operatorname{diag}}
\newcommand{\blkdiag}{\operatorname{blkdiag}}
\newcommand{\clip}{\operatorname{clip}}
\newcommand{\median}{\operatorname{median}}
\newcommand{\coll}{\operatorname{col}}
\newcommand{\cond}{\operatorname{cond}}
\newcommand{\unvec}{\operatorname{unvec}}

\begin{document}

\title{Maxwell--Jones Vector-Sensor Localization and Synchronization for
Distributed Near-Field RIS-Aided OFDM}

\author{Qingshan Gong,~\IEEEmembership{Member,~IEEE}}

\markboth{IEEE Transactions on Wireless Communications,~Vol.~XX, No.~XX, 2026}%
{Gong: Maxwell--Jones Vector-Sensor Localization and Synchronization}

\maketitle

\begin{abstract}
We study joint 3-D localization and synchronization of a single-antenna user 
in a wideband orthogonal frequency-division multiplexing (OFDM) uplink in 
which the direct path is blocked and a single electromagnetic vector-sensor (EVS) 
array observes the user only through distributed near-field reconfigurable intelligent surfaces (RISs). 
All cascaded paths share an unknown clock offset, with unknown gains, Jones coefficients 
and panel associations. We propose a two-phase estimator built on a per-panel 
space--frequency/Maxwell--Jones factorization. Phase~I (MKSC-GI) applies a calibrated sufficient Maxwell--Kronecker compression that reduces the receive dimension from $96$ to $6$, recovers the delay factors and their 
panel associations, and fuses the panel-local near-field estimates into a common position initializer. 
Phase~II (CCOP-JVP) eliminates the Jones coefficients by variable projection and profiles the 
one-dimensional clock orbit by certified branch-and-bound, leaving a three-dimensional position refinement. 
The same factorization yields three structural results: receiver selection rescales single-panel free-Jones 
information only through the retained polarization energy, though two or fewer retained components can void 
panel separability; the compression preserves the profiled optimizer and the free-Jones information; 
and the clock profile is a finite trigonometric polynomial. At $-10$~dB SNR the estimator achieves 
a $0.31$-mm median position error with $0.42\%$ position and $0.21$ clock catastrophic-event rates, 
while the closest in-model 4-D control has comparable median accuracy but has a $1.98\%$ position catastrophic rate.
\end{abstract}

\begin{IEEEkeywords}
RIS, EVS, near-field localization, clock synchronization, OFDM, polarimetric
sensing, variable projection, branch-and-bound, sufficient statistics, Fisher information.
\end{IEEEkeywords}


\section{Introduction}
\label{sec:introduction}

\IEEEPARstart{R}{econfigurable} intelligent surfaces (RISs) make the
propagation environment itself programmable, and are studied as a low-cost
way to shape coverage in future wireless systems~\cite{wymeersch2020risloc}.
As their apertures grow electrically large at millimeter-wave and
sub-terahertz frequencies, the users they serve fall inside the radiative
near field, where the plane-wave approximation no longer holds and the
curvature of the wavefront across the aperture carries range in addition to
direction~\cite{cui2023nearfield}. That extra geometric
content turns an electrically large surface from a communication device into
a passive anchor, able to fix a user in three dimensions without active
infrastructure of its own~\cite{elzanaty2021bounds}. Deploying several such
panels adds spatially diverse reflected paths, which matters most when the
direct link to the base station (BS) is obstructed. A passive RIS provides
no independent timing reference, however, so the delays measured through such
a deployment carry
the clock offset of the user equipment (UE) as well as its position, and the
two have to be recovered together. This joint problem lies at the
intersection of three largely separate lines of work: RIS-aided
localization and synchronization, near-field and
extremely-large-aperture estimation, and polarimetric sensing.

The first line, RIS-aided localization and synchronization, has moved from
single-panel geometry to distributed
deployments~\cite{ma2023distributed,liu2025multiris} and to formulations
that carry an unknown clock
explicitly~\cite{fascista2022rissync,gong2024async,peng2025sdp,yan2025ris}.
The second line is near-field and
extremely-large-aperture estimation, which supplies both the spherical-wave
models this regime needs and the acquisition front ends that make them
tractable: multilinear factorization of the training
tensor~\cite{lin2021tensor}, sparse and variational recovery on a
discretized manifold~\cite{li2024vbi}, and polar or beam-split codebooks
matched to wavefront
curvature~\cite{cui2022channel,yang2024nfris,zheng2025wideband}.
The third line is polarimetric sensing, where polarization enters RIS work
mainly as a controllable resource -- dual-polarized surfaces spend it on
beamforming gain~\cite{han2022dual} -- whereas array
processing treats it as an unknown to be estimated: an electromagnetic vector
sensor (EVS) measures co-located electric and magnetic fields and so resolves
the polarization state of an incident wave alongside its
direction~\cite{nehorai1994vector,khan2020sixcomponent}. Our own EVS-OFDM
(orthogonal frequency-division multiplexing)
estimator~\cite{gong2026aimdf} applies such an array to a direct multipath
channel, with none of the distributed cascaded geometry, panel labeling,
compression or shared-clock structure at issue here.

Despite these advances, the resulting estimation problem remains unresolved
because the limitations of the three lines compound in this architecture.
First, distributed RIS localization and synchronization typically resolve
panel identities before or during the joint
fit~\cite{ma2023distributed,liu2025multiris,fascista2022rissync}. Here, all
cascades reach the same EVS array within one observation and an algebraic
front end returns them unlabeled~\cite{lin2021tensor}; two panels at nearly
coincident cascaded delays present increasingly similar space--frequency
evidence. Their separability then also depends on the retained Maxwell
structure of the receiver, whereas existing polarimetric processing mainly
treats polarization as an additional estimation
dimension~\cite{nehorai1994vector,khan2020sixcomponent}. Second, acquisition
becomes most demanding in the large-aperture wideband regime that motivates
near-field processing: full-EVS training produces six field components over
$M_A$ sensors, $N$ subcarriers and $T$ RIS configurations, while an arbitrary
linear reduction provides no guarantee of preserving the delay subspace,
profiled likelihood or localization information. Fixed discretizations used by
sparse and codebook-based front ends~\cite{li2024vbi,cui2022channel} also
become increasingly coarse relative to the aperture resolution, while
restoring that resolution requires a denser dictionary and a larger search
burden. Third, the shared clock is either discarded or retained as an extra
nonlinear variable. Differencing cascaded OFDM delays removes the common
offset but leaves only $K-1$ differences for three position coordinates, which
is underdetermined at $K=3$, whereas retaining the absolute delays leaves a
nonconvex four-dimensional position--clock problem. Variable projection
eliminates the linear nuisances but not this coupling~\cite{golub1973varpro},
while the semidefinite route to a certified global solution~\cite{peng2025sdp}
addresses timing models that lift to a linear one, not the OFDM likelihood
here. The closest formulations reflect these limits: the nearest wideband
near-field RIS work estimates the channel without a
clock~\cite{yang2024nfris}, whereas the nearest near-field
localization-and-synchronization work considers one surface and a scalar
receive link~\cite{yan2025ris}. The architecture considered here exposes all
three limitations simultaneously.

We address these limitations through one structural property of the
cascaded observation rather than through three separate devices. The sensing block of
the $k$th RIS panel factorizes as
\begin{equation}
	\bA_k(\bchi)=
	\underbrace{\bc_k(\bchi)}_{\text{space--frequency geometry}}
	\otimes
	\underbrace{\bTheta_k^{({\rm mode})}}_{\text{calibrated Maxwell--Jones}},
	\label{eq:premise}
\end{equation}
where $\bchi=[\bp^{\mathsf T},\Delta t]^{\mathsf T}$ collects the unknown
position and common clock, $\bc_k$ carries all of that dependence, and
$\bTheta_k^{({\rm mode})}$ -- two columns for the polarization state, one row
per retained field component -- is fixed by the calibrated RIS-to-BS geometry
and the receiver selection. The unknown geometry and clock are therefore
separated from the calibrated polarimetric response. Three structural
consequences follow, and together they address the limitations above.
First, \emph{Maxwell--Jones information splitting}: the Kronecker form
splits the orthogonal projector into a geometric and a polarimetric term, so
receiver selection reweights a panel's free-Jones information only through
the polarization energy it retains and leaves the geometric kernel unchanged;
panel separability meanwhile obeys a separate subspace condition, and when
two or fewer field components are retained and the competing retained Jones
block has full row rank, panels sharing a space--frequency signature can lose
polarimetric separability entirely (C1, Theorem~\ref{thm:split}). Second,
\emph{Sufficient Maxwell--Kronecker compression}: the calibrated Maxwell
factors confine every admissible response to a parameter-independent receive
subspace of dimension at most twice the panel count, and projection onto that
subspace preserves the profiled likelihood maximizer, the parameter
derivatives of the residual criterion and the free-Jones information exactly
-- together with the noiseless delay poles, under the stated snapshot-rank
condition -- so acquisition proceeds on a representation that discards only
noise-only receive directions (C2, Theorem~\ref{thm:compress}). Third,
\emph{Certified common-clock profiling}: the shared clock acts as a common
unitary OFDM phase ramp that leaves the Jones Gram matrix invariant, reducing
the conditional clock dependence to a finite trigonometric polynomial whose
one-dimensional global maximization can be certified with a deterministic
optimality gap; the outer search then drops from four dimensions to three and
becomes invariant to the clock unit, though it remains local, the certificate
covering the conditional clock solve alone (C3, Theorem~\ref{thm:clock}).

Together these results give a two-phase estimator, though they do not enter
it in the same way. C2 is what the implementation runs on: it defines the
compression on which Phase~I builds a physically associated near-field
initializer, and it licenses Phase~II to evaluate the exact forward model on
that same compressed observation without approximation. C3 supplies
Phase~II's refinement, in which the Jones coefficients are eliminated
analytically by variable projection and the certified one-dimensional clock
profile removes the clock from the outer search, leaving a three-dimensional
position refinement regularized only in the Jones directions a reduced
receiver cannot see. C1 is not an algorithmic ingredient but a condition on
the receiver, stating which reduced EVS configurations retain enough
polarization structure for panel association to remain well posed.
Section~\ref{sec:simulation} tests that condition directly and, to
distinguish local precision from basin reliability, evaluates the
catastrophic tail alongside conditional precision.

Notation: bold symbols denote vectors/matrices; $(\cdot)^{\mathsf T}$,
$(\cdot)^{\mathsf H}$ and $(\cdot)^{\dagger}$ are transpose, Hermitian
transpose and pseudoinverse; $\otimes$ and $\odot$ are Kronecker and
Hadamard products; for a generic matrix $\bX$, $\bP_{\bX}=\bX\bX^{\dagger}$
and $\bPi_{\bX}^{\perp}=\bI-\bP_{\bX}$ are the orthogonal projectors onto
$\coll(\bX)$ and $\coll(\bX)^{\perp}$; and unqualified vector and matrix
norms are Euclidean and spectral, respectively.

\section{Maxwell--Jones Cascaded Signal Model}
\label{sec:system_model}

We consider a blocked line-of-sight (LoS) wideband uplink in which a
single-antenna UE reaches an $M_A$-element EVS uniform linear array (ULA) at
the BS only through $K$ distributed RIS panels of $M_R=M_{Rx}M_{Ry}$ elements
each~\cite{fascista2022rissync,zhao2021timing}. Each panel contributes one
dominant cascaded component, so the model order equals the known panel count
$K$. The UE transmits pilots on $N$ subcarriers spaced
by $\Delta f$, giving an occupied bandwidth $B_{\rm occ}=(N-1)\Delta f$, over
$T$ known RIS phase configurations that all lie within one coherence block.
Following the one-sided near-field convention
of~\cite{yang2024nfris,cui2022channel}, the UE--RIS hop is modeled by an
exact spherical-wave phase over a uniform-amplitude aperture and the
calibrated RIS--BS hop by a center-frequency plane wave, conditions
Section~\ref{sec:simulation} verifies for the reference geometry of
Fig.~\ref{fig:geometry}(a).

The received signal is
\begin{equation}
	\by=\sum_{k=1}^{K}\Big[
	\underbrace{\bh_k(\bp)}_{\text{near field}}\otimes
	\underbrace{\bd_k(\bp,\Delta t)}_{\text{delay}}\otimes
	\underbrace{\bBm_k}_{\text{calibrated}}\Big]\bx_k+\bn,
	\label{eq:roadmap}
\end{equation}
with $\by,\bn\in\C^{M}$, $M=I\,N\,T$ for the receive dimension $I$ of
Section~\ref{subsec:calibrated}, and $\bx_k\in\C^2$ a free Jones coefficient;
only $\bh_k$ and $\bd_k$ depend on the unknown $(\bp,\Delta t)$. The three
factors are derived next.

\begin{figure*}[!t]
\centering
\includegraphics[width=\textwidth]{EVS_RIS_system_model-crop.pdf}
\caption{Reference geometry and cascaded-path parameterization.
(a)~Blocked-LoS deployment: $K=3$ oriented $64\times64$ RIS panels relay the
UE signal to a $16$-element EVS ULA, with a spherical UE--RIS wavefront, a
center-frequency plane wave on RIS--BS, and the single clock offset
$\Delta t$ shared by all $K$ paths. (b)~Local frame of the $k$th panel:
$\bxi_k=\bR_k(\bp-\br_k)$, the element offset $\brho_{k,m}$, the exact
wavefront difference $\delta_{k,m}$ of \eqref{eq:delta}, and the transverse
pair $(\bq_{k,1},\bq_{k,2})$ of \eqref{eq:qbasis} carrying the Maxwell--Jones
block. (c)~Six-component EVS element and the receiver selections
$\bS_1,\bS_2,\bS_6$ of \eqref{eq:smode}.}
\label{fig:geometry}
\end{figure*}

\subsection{Calibrated RIS--BS Maxwell Factor}
\label{subsec:calibrated}

Deployment calibration fixes every quantity entering the UE-independent
receive factor of panel $k$: the BS phase center $\bp_B$, the RIS panel
centers $\br_k$ and rotations $\bR_k\in{\rm SO}(3)$ mapping global
coordinates into the $k$th panel frame, the element coordinates
$\brho_{k,m}\in\R^3$ in that frame, and the BS array geometry and
EVS Maxwell response. The BS ULA lies along the global $x$ axis of the
orthonormal frame $\{\bm u_x,\bm u_y,\bm u_z\}$ and
$\kappa=2\pi/\lambda_c$ is the center-frequency wavenumber.

The calibrated locations give the RIS--BS distance and propagation direction
of panel $k$,
\begin{equation}
	d_{{\rm RB},k}=\|\bp_B-\br_k\|_2,
	\qquad
	\bs_{{\rm RB},k}=\frac{\bp_B-\br_k}{d_{{\rm RB},k}}
	\label{eq:rbgeom}
\end{equation}
and, under the far-field RIS--BS model established above, a plane-wave panel
response and a BS steering vector,
\begin{align}
	\big[\ba_{{\rm RB},k}\big]_m
	&=e^{-j\kappa\,\brho_{k,m}^{\mathsf T}\bR_k\bs_{{\rm RB},k}},\nonumber\\
	\big[\bv_{{\rm B},k}\big]_i
	&=e^{j\kappa x_i\,\bm u_x^{\mathsf T}\bs_{{\rm RB},k}},
	\label{eq:arb_vb}
\end{align}
with $x_i$ the $x$ coordinate of the $i$th BS element,
$\ba_{{\rm RB},k}\in\C^{M_R}$ and $\bv_{{\rm B},k}\in\C^{M_A}$.

The same direction fixes the polarization manifold: a transverse plane wave
along $\bs_{{\rm RB},k}$ has Maxwell pair
$(\bE,\bH)=(\bq,\bs_{{\rm RB},k}\times\bq)$ in impedance-normalized units, so
with any transverse basis
\begin{equation}
	\bq_{k,1}\in\bs_{{\rm RB},k}^{\perp},
	\quad \|\bq_{k,1}\|_2=1,
	\qquad
	\bq_{k,2}=\bs_{{\rm RB},k}\times\bq_{k,1},
	\label{eq:qbasis}
\end{equation}
the six-component
vector-sensor manifold~\cite{nehorai1994vector,khan2020sixcomponent}
specializes to the calibrated Maxwell--Jones block
\begin{equation}
	\bTheta_k^{(6)}=\begin{bmatrix}
	\bq_{k,1} & \bq_{k,2}\\[1pt]
	\bq_{k,2} & -\bq_{k,1}\end{bmatrix}\in\C^{6\times2},
	\label{eq:Theta}
\end{equation}
whose columns satisfy
$[\bTheta_k^{(6)}]^{\mathsf H}\bTheta_k^{(6)}=2\bI_2$; a rotation
$\bU\in{\rm SO}(2)$ of the transverse basis sends
$\bTheta_k^{(6)}\mapsto\bTheta_k^{(6)}\bU$ and is absorbed by
$\bx_k\mapsto\bU^{\mathsf T}\bx_k$. Deployment
calibration is assumed to equalize per-component gains and cross-channel
coupling, so \eqref{eq:Theta} is the post-calibration EVS response. A fixed
receiver selection $\bS_{\rm mode}\in\R^{d_{\rm evs}\times6}$,
\begin{equation}
	\bS_1=\begin{bmatrix}1 & \mathbf 0\end{bmatrix},
	\quad
	\bS_2=\begin{bmatrix}\bI_2 & \mathbf 0\end{bmatrix},
	\quad
	\bS_6=\bI_6,
	\label{eq:smode}
\end{equation}
with $d_{\rm evs}\in\{1,2,6\}$ for the scalar, dual-polarized and full-EVS
receivers of Fig.~\ref{fig:geometry}(c), gives
$\bTheta_k^{({\rm mode})}\triangleq\bS_{\rm mode}\bTheta_k^{(6)}
\in\C^{d_{\rm evs}\times2}$ and the receive dimension $I=d_{\rm evs}M_A$.

The calibrated receive block
$\bBm_k\triangleq\bv_{{\rm B},k}\otimes\bTheta_k^{({\rm mode})}\in\C^{I\times2}$
is therefore known before UE estimation and satisfies
$\partial\bBm_k/\partial\bchi=\mathbf0$; only the Jones-domain coefficients
$\bx_k\in\C^2$ of \eqref{eq:roadmap} remain unknown, each being the effective
polarization state launched toward the BS after reflection rather than the UE
antenna state.

\subsection{UE--RIS Near-Field and Common-Clock Factor}
\label{subsec:defchain}

The unknown nonlinear parameter collects the UE position and the single
clock, $\bchi=[\bp^{\mathsf T},\Delta t]^{\mathsf T}
=[x,y,z,\Delta t]^{\mathsf T}\in\R^4$, with $\bp\in\mathcal P$ a known closed
service box and $\Delta t\in\mathcal T$ a known closed interval,
$|\mathcal T|<1/\Delta f$ the unambiguous delay period. Every cascaded delay
is a geometric path length plus this one
offset~\cite{fascista2022rissync,gong2024async,yan2025ris};
panel-dependent offsets~\cite{zhao2021timing} lie outside the model.
In the $k$th panel frame (Fig.~\ref{fig:geometry}(b)),
$\bxi_k(\bp)=\bR_k(\bp-\br_k)$ and
\begin{equation}
	d_{{\rm UR},k}(\bp)=\|\bxi_k(\bp)\|_2=\|\bp-\br_k\|_2,
	\quad
	\bs_{{\rm UR},k}(\bp)=\frac{\bxi_k(\bp)}{d_{{\rm UR},k}(\bp)},
	\label{eq:urgeom}
\end{equation}
the second equality because $\bR_k$ is a rotation. Element $m$ of panel $k$
applies the scalar Jones response $e^{j\omega_{k,t,m}}\bI_2$ at configuration
$t$, so no polarization mixing occurs; with the prescribed constant-modulus
training $[\bOmega_k]_{t,m}=e^{j\omega_{k,t,m}}/\sqrt{M_R}$,
$\bOmega_k\in\C^{T\times M_R}$~\cite{lin2021tensor}, the near-field
dependence is captured by the RIS response
\begin{align}
	\bg_k(\bp) &= \ba_{{\rm RB},k}\odot\ba^{\rm NF}_{{\rm UR},k}(\bp)
	\in\C^{M_R},\nonumber\\
	\bh_k(\bp) &= \bOmega_k\,\bg_k(\bp)\in\C^{T},
	\label{eq:hk}
\end{align}
where
\begin{align}
	\big[\ba^{\rm NF}_{{\rm UR},k}(\bp)\big]_m
	&= e^{-j\frac{2\pi}{\lambda_c}\delta_{k,m}(\bp)},
	\quad \ba^{\rm NF}_{{\rm UR},k}\in\C^{M_R},\nonumber\\
	\delta_{k,m}(\bp)
	&= \big\|\bxi_k(\bp)-\brho_{k,m}\big\|_2
	- d_{{\rm UR},k}(\bp).
	\label{eq:delta}
\end{align}
Equation~\eqref{eq:delta} retains the exact spherical-wave phase across the
panel aperture (Fig.~\ref{fig:geometry}(b)); the Fresnel approximation enters
only in Phase-I proposal generation (Section~\ref{subsec:beamspace}).

Combining \eqref{eq:rbgeom} and \eqref{eq:urgeom}, the cascaded delay and its
OFDM phase ramp on the tone grid
$f_n=f_c+\big(n-\tfrac{N-1}{2}\big)\Delta f$, $n=0,\ldots,N-1$, are
\begin{align}
	\tau_k(\bp,\Delta t)
	&= \underbrace{\frac{d_{{\rm UR},k}(\bp) + d_{{\rm RB},k}}{c_0}}_{
	\textstyle \tau_{g,k}(\bp)} + \Delta t,\nonumber\\
	\bd_k(\bp,\Delta t)
	&= \Big[\,e^{-j2\pi\left(n-\frac{N-1}{2}\right)\Delta f\,
	\tau_k(\bp,\Delta t)}\,\Big]_{n=0}^{N-1}\in\C^N,
	\label{eq:dk}
\end{align}
with $c_0$ the speed of light and the tone-independent phase
$e^{-j2\pi f_c\tau_k}$ absorbed into the path gain. The per-subcarrier form of \eqref{eq:dk}
assumes the cyclic prefix covers the cascaded delay spread and neglects
residual carrier-frequency offset and phase noise. Unlike the
frequency-dependent spatial manifold of~\cite{yang2024nfris}, spatial
manifolds are evaluated here at the carrier $f_c$ and the wideband dependence
is carried by the delay phase on every subcarrier. With $\Delta L$ an
inter-element path difference of a spatial response, the neglected
spatial-wideband phase
$\epsilon_{\rm sq}=\pi B_{\rm occ}\max|\Delta L|/c_0$ peaks at $0.102$~rad
over the three spatial responses of the reference configuration (design
condition and per-panel audit in the supplement).

The bracketed factor of \eqref{eq:roadmap} is the per-panel sensing block,
which by associativity of the Kronecker product also takes the
form~\eqref{eq:premise},
\begin{align}
	\bA_k(\bchi)
	&= \bh_k(\bp)\otimes\bd_k(\bp,\Delta t)\otimes\bBm_k\nonumber\\
	&= \bc_k(\bchi)\otimes\bTheta_k^{({\rm mode})}\in\C^{M\times2},
	\label{eq:Ak}
\end{align}
where $\bc_k(\bchi)=\bh_k(\bp)\otimes\bd_k(\bp,\Delta t)\otimes
\bv_{{\rm B},k}\in\C^{TNM_A}$ is the space--frequency factor. Entry
$(t-1)NI+nI+i$ of $\by$ carries receive channel $i=1,\ldots,I$ and subcarrier
$n=0,\ldots,N-1$ at configuration $t=1,\ldots,T$; the scalar sample expansion
and pilot normalization are in the supplement.

Equation~\eqref{eq:Ak} admits two useful groupings. The first,
$\bh_k\otimes\bd_k\otimes\bBm_k$, exposes the calibrated receive subspace:
since
$\bA_k\bx_k=\bh_k\otimes\bd_k\otimes(\bBm_k\bx_k)$, reading the $I$ receive
channels against the $N$ subcarriers at configuration $t$ arranges the same
samples as
\begin{equation}
	\bY_t=\sum_{k=1}^{K}[\bh_k(\bp)]_t\,(\bBm_k\bx_k)\,
	\bd_k(\bp,\Delta t)^{\mathsf T}+\bN_t,
	\label{eq:obsmat}
\end{equation}
$t=1,\ldots,T$, with $\bY_t\in\C^{I\times N}$ and $\bN_t$ the noise block. Each panel is therefore rank
one across receive and frequency at fixed $t$, its receive factor confined to
the $\bchi$-independent $\coll(\bBm_k)$ of dimension at most two. The second
grouping,
$\bc_k\otimes\bTheta_k^{({\rm mode})}$, confines all dependence on
$\bS_{\rm mode}$ to $\bTheta_k^{({\rm mode})}$ and all position--clock
dependence to the mode-independent $\bc_k(\bchi)$, with $\Delta t$ entering
only through $\bd_k$. Section~\ref{sec:algorithm} works in the first
arrangement and Section~\ref{sec:analysis} in the second.

\subsection{Observation and Estimation Problem}
\label{subsec:obsmodel}

Stacking the panels as
$\bA(\bchi)=[\bA_1(\bchi),\ldots,\bA_K(\bchi)]\in\C^{M\times 2K}$ and
vectorizing the $\bY_t$ in the same order writes \eqref{eq:roadmap} as
\begin{equation}
	\by = \bmu(\bchi,\bx) + \bn = \bA(\bchi)\,\bx + \bn,
	\qquad
	\bn\sim\mathcal{CN}(\mathbf 0,\sigma^2\bI_M),
	\label{eq:obs}
\end{equation}
where the noise is white across receive channels, subcarriers and RIS
configurations, with power $\sigma^2$ \emph{not} provided to the estimator. The unknown Jones-domain coefficient of path $k$
is $\bx_k=\beta_k\be_k\in\C^2$, stacked as
$\bx=[\bx_1^{\mathsf T},\ldots,\bx_K^{\mathsf T}]^{\mathsf T}\in\C^{2K}$,
where the complex gain $\beta_k$ absorbs path loss, RIS efficiency and the
center-frequency phase of the cascade and $\be_k$ is a unit Jones state
(angular parameterization in the supplement). Both factors are flat over the
occupied band and only their product is identifiable, so the estimator
treats $\bx_k$ as a free $\C^2$ nuisance.

Profiling $\sigma^2$ out of the white-Gaussian log-likelihood gives
$\widehat\sigma^2=\|\by-\bA(\bchi)\bx\|_2^2/M$ and a concentrated criterion
strictly decreasing in the residual energy, so maximum-likelihood (ML)
estimation of $(\bchi,\bx)$ is the least-squares problem
\begin{equation}
	\min_{\bp\in\mathcal P,\ \Delta t\in\mathcal T,\ \bx\in\C^{2K}}
	\big\|\by-\bA(\bp,\Delta t)\,\bx\big\|_2^2.
	\label{eq:ml_problem}
\end{equation}
The $2K$ Jones coefficients enter linearly and admit closed-form
elimination, whereas position and clock enter nonlinearly through
\eqref{eq:delta} and \eqref{eq:dk}, leaving \eqref{eq:ml_problem} nonconvex
in four real coordinates.

\section{Proposed Estimator: Sufficient Acquisition and Certified Clock Profiling}
\label{sec:algorithm}

Direct optimization of \eqref{eq:ml_problem} couples the panel
association, the near-field basin, the $2K$ linear Jones coefficients and
the common clock. We separate them with a two-phase estimator. Phase~I,
\emph{Maxwell--Kronecker sufficient compression with common-geometry
initialization} (MKSC-GI), recovers and associates the cascaded factors
inside a calibration-fixed sufficient subspace and fuses the panel-local
estimates into one common position initializer. Phase~II,
\emph{common-clock orbit-profiled Jones variable projection} (CCOP-JVP),
eliminates the Jones coefficients analytically and refines position and
clock by certified profiling along the exact clock orbit.

\subsection{Maxwell--Kronecker Factor Recovery}

\textit{1) Sufficient compression and delay-pole recovery:}
Position and clock enter each cascaded delay only through their sum, so no
single cascaded delay separates them and Phase~I begins from the delay poles.
Two structural facts of \eqref{eq:Ak} make those poles recoverable from
\eqref{eq:obsmat} with no knowledge of $\bchi$.

First, the receive dimension reduces without loss. By \eqref{eq:Ak} every
column of $\bY_t$ is a combination of the calibrated vectors $\bBm_k\bx_k$,
and $\bBm_k=\bv_{{\rm B},k}\otimes\bTheta_k^{({\rm mode})}$ has two columns,
so all admissible receive responses lie in
\begin{equation}
	\mathcal S_R=\sum_{k=1}^{K}\coll(\bBm_k)=\coll(\bPsi),
	\quad
	\bPsi=[\bBm_1,\ldots,\bBm_K],
	\label{eq:calibsub}
\end{equation}
of dimension $r=\rank\bPsi\le2K$, while the observation carries
$I=d_{\rm evs}M_A$ receive channels: the reduction is from $I=96$ to
$r\le6$ in the reference scene, and the $I-r$ orthogonal receive directions
carry noise but no signal. Because $\bPsi$ is built from calibrated
quantities alone, so is any orthonormal basis $\bQ_S\in\C^{I\times r}$ of
$\mathcal S_R$, and $\bQ_S\bQ_S^{\mathsf H}\bBm_k=\bBm_k$ for every $k$.
Compressing the receive index alone gives
\begin{equation}
	\bY_{{\rm d},t}=\bQ_S^{\mathsf H}\bY_t
	=\sum_{k=1}^{K}[\bh_k(\bp)]_t\,\ba_k\,
	\bd_k(\bp,\Delta t)^{\mathsf T}+\bQ_S^{\mathsf H}\bN_t,
	\label{eq:obsmat_c}
\end{equation}
with $\ba_k=\bQ_S^{\mathsf H}\bBm_k\bx_k\in\C^{r}$: this is
\eqref{eq:obsmat} in receive dimension $r$ rather than $I$, with
$\bQ_S^{\mathsf H}\bN_t$ still white and nothing lost by
Theorem~\ref{thm:compress}.

Second, the delay of path $k$ reaches the observation only through the
single pole $z_k=e^{-j2\pi\Delta f\tau_k}$, so its OFDM ramp is the geometric
sequence $[\bd_k]_n=e^{j\pi(N-1)\Delta f\tau_k}z_k^{\,n}$,
$n=0,\ldots,N-1$: the centered tone grid contributes a nonzero per-column
scalar only, which changes neither $\coll(\bd_k)$ nor the shift invariance
below. Reading the
$rT$ compressed channel--configuration pairs of the $\bY_{{\rm d},t}$ as
snapshots against the $N$ tones gives the frequency matrix
\begin{equation}
	\bY_f=\sum_{k=1}^{K}\bd_k\,\bw_k^{\mathsf T}+\bN_f\in\C^{N\times rT},
	\qquad
	\bw_k=\ba_k\otimes\bh_k(\bp),
	\label{eq:freqsnap}
\end{equation}
in which every dependence on $\bchi$ outside $\bd_k$ is confined to the
snapshot vector $\bw_k$. Writing $\bD=[\bd_1,\ldots,\bd_K]$ and
$\underline{\bD}$, $\overline{\bD}$ for $\bD$ without its last and first
row, the $K$ poles obey an exact shift invariance on the tone index,
\begin{equation}
	\overline{\bD}=\underline{\bD}\,\boldsymbol\Phi,
	\qquad
	\boldsymbol\Phi=\diag(z_1,\ldots,z_K),
	\label{eq:shift}
\end{equation}
which subspace methods read off directly~\cite{roy1989esprit,gong2026aimdf}.
Under cascaded delays distinct modulo $1/\Delta f$, at least $K+1$ tones and
the snapshot-rank condition of Theorem~\ref{thm:compress}(iv) applied to
$[\bw_1,\ldots,\bw_K]$, the $K$ delays are thus identifiable from the
frequency axis alone.

\textit{2) Coupled factor recovery:}
Forward--backward total-least-squares estimation of signal parameters via
rotational invariance techniques
(ESPRIT)~\cite{roy1989esprit,gong2026aimdf} then recovers
$\boldsymbol\Phi$ of \eqref{eq:shift} from $\bY_f$, each estimated pole
projected onto the unit circle. Once the $\hat z_k$ are fixed, $\bD$ is known
and the coupled columns $\bw_k=\ba_k\otimes\bh_k$ of \eqref{eq:freqsnap}
enter linearly. Collecting them as $\bF=[\bw_1,\ldots,\bw_K]\in\C^{rT\times K}$
and writing $\bY_{rT,N}\in\C^{rT\times N}$ for the rearrangement of the
$\bY_{{\rm d},t}$ that groups the compressed receive and training indices as
rows against the tone index, they solve one loaded weighted least-squares
problem on the tone axis,
\begin{equation}
	\hat\bF=\arg\min_{\bF}\
	\big\|(\bY_{rT,N}-\bF\bD^{\mathsf T})\bW^{1/2}\big\|_F^2
	+\lambda_\tau\|\bF\|_F^2,
	\label{eq:coupledls_obj}
\end{equation}
whose stationarity condition
$\bF(\bD^{\mathsf T}\bW\bD^{\ast}+\lambda_\tau\bI)=\bY_{rT,N}\bW\bD^{\ast}$
has the closed form
\begin{equation}
	\hat\bF=\bY_{rT,N}\bW\bD^{\ast}
	(\bD^{\mathsf T}\bW\bD^{\ast}+\lambda_\tau\bI)^{-1}.
	\label{eq:coupledls_short}
\end{equation}
Here $\lambda_\tau$ is the loading fixed in the supplement and
$\bW=\diag(w_0,\ldots,w_{N-1})$ carries the frequency-smoothing weights,
$w_n$ the anti-diagonal multiplicity $\#\{(p,\ell):p+\ell=n\}$ of a
$P\times L$ window with $P+L-1=N$: smoothing enters \eqref{eq:coupledls_obj}
as a tone weighting, and all $K$ coupled columns follow from one $K\times K$
solve. Separating the two factors inside a recovered column is a local
operation. Let $\unvec_{r\times T}(\cdot)$ reshape a column of $\C^{rT}$ into
$\C^{r\times T}$ with the compressed receive index running slowest, so that
$\unvec_{r\times T}(\ba\otimes\bt)=\ba\bt^{\mathsf T}$, and let
$\unvec_{r\times T}(\hat\bF_{:,j})=\sum_i\sigma_{i,j}\bu_{i,j}
\bv_{i,j}^{\mathsf H}$ be its singular value decomposition. Then
\begin{equation}
	\tilde\ba_j=\sqrt{\sigma_{1,j}}\,\bQ_S\bu_{1,j},
	\quad
	\tilde\bt_j=\sqrt{\sigma_{1,j}}\,\bv_{1,j}^{\ast},
	\quad
	\varrho_j=\frac{\sigma_{2,j}}{\sigma_{1,j}},
	\label{eq:split_rank1}
\end{equation}
splits the leading singular value symmetrically between
$\tilde\ba_j\in\C^{I}$ and $\tilde\bt_j\in\C^{T}$.

Everything geometric below consumes these two vectors, so it is worth
recording what they are. Let $k$ be the panel that produced column $j$,
unknown until the association step next. Its noiseless column is
$\bw_k=\ba_k\otimes\bh_k(\bp)$ of \eqref{eq:freqsnap}, and the chosen
ordering makes $\unvec_{r\times T}(\bw_k)=\ba_k\bh_k^{\mathsf T}(\bp)$ rank
one, so $\bu_{1,j}\propto\ba_k$ and $\bv_{1,j}^{\ast}\propto\bh_k(\bp)$.
Substituting $\ba_k=\bQ_S^{\mathsf H}\bBm_k\bx_k$ and using the exactness
$\bQ_S\bQ_S^{\mathsf H}\bBm_k=\bBm_k$ of \eqref{eq:calibsub},
\begin{equation}
	\tilde\ba_j=c_j\,\bBm_k\bx_k,
	\qquad
	\tilde\bt_j=c_j'\,\bh_k(\bp),
	\label{eq:factor_meaning}
\end{equation}
with $c_j,c_j'\neq0$ set by the singular-value split and the singular-vector
phase. The front end thus returns, per path, the calibrated receive vector
and the near-field RIS response of one panel, each only up to scale. Two
consequences organize the rest of Phase~I: the panel label must be recovered
separately, which is the next step; and every criterion built on
$\tilde\ba_j$ or $\tilde\bt_j$ has to be scale invariant, which is why
\eqref{eq:assoc_score}, \eqref{eq:Jris_short} and \eqref{eq:Jgeom_short} are
all projection residuals. Noise breaks the exact rank-one form and
$\varrho_j$ measures by how much, so it serves as the per-column confidence
weighting the common-geometry fit below.

\textit{3) Physical panel association:}
Panel association is performed before any geometry is fused, by searching
permutations under a
composite cost that combines three complementary cues: Maxwell-subspace
consistency identifies the panel-dependent receive manifold, spherical-wave
consistency tests the panel-local geometry, and the common-clock replicas
enforce a joint cross-panel constraint. For column $j$ and panel $k$,
\begin{equation}
 s_{j,k}=
 \frac{\|\bPi^{\perp}_{\bBm_k}\tilde\ba_j\|}{\|\tilde\ba_j\|}
 +\min_{\boldsymbol\eta\in\mathcal E_k}
 \frac{\|\bPi^{\perp}_{\bh_k(\boldsymbol\eta)}\tilde\bt_j\|}
 {\|\tilde\bt_j\|},
 \label{eq:assoc_score}
\end{equation}
with $\bBm_k$ the calibrated receive block of panel $k$ defined in
Section~\ref{subsec:calibrated}, $\bh_k(\boldsymbol\eta)$ the exact
near-field panel response \eqref{eq:hk} in the panel-local coordinates
$\boldsymbol\eta_k=(d_{{\rm UR},k},u_k,v_k)$ -- range and the first two
components of $\bs_{{\rm UR},k}$, in bijection with $\bxi_k$ in front of the
panel -- and $\mathcal E_k$ the local search box over them. Both summands
are dimensionless relative residuals, so $0\le s_{j,k}\le2$. Writing $\hat d_{j,k}$ for the range coordinate of the minimizer
in \eqref{eq:assoc_score} and $\hat\tau_j$ for the delay of the estimated
pole, each pair also implies a clock replica
$\widehat{\Delta t}_{j,k}=\hat\tau_j-(\hat d_{j,k}+d_{{\rm RB},k})/c_0$,
and a permutation is scored by
\begin{equation}
 C(\pi)=\sum_{j=1}^{K}s_{j,\pi(j)}
 +\frac{w_{\rm clk}}{t_0}
 \big[\,\mathrm{sd}(\pi)+e_{\mathcal T}(\pi)\big],
 \label{eq:assoc_perm}
\end{equation}
where $e_{\mathcal T}(\pi)$ is the mean excursion of the $K$ replicas
outside $\mathcal T$, $w_{\rm clk}=1/2$, $t_0=1$~ns, and
\begin{align}
	\mathrm{sd}(\pi)&=\Big[\tfrac1K\sum_{j=1}^{K}
	\big(\widehat{\Delta t}_{j,\pi(j)}-\overline{\Delta t}(\pi)\big)^2
	\Big]^{1/2},\nonumber\\
	\overline{\Delta t}(\pi)&=\tfrac1K\sum_{j=1}^{K}
	\widehat{\Delta t}_{j,\pi(j)}.
	\label{eq:assoc_sd}
\end{align}
The first sum of \eqref{eq:assoc_perm} is separable over the pairs
$(j,\pi(j))$, but \eqref{eq:assoc_sd} is not: its center
$\overline{\Delta t}(\pi)$ moves with every assignment, which is precisely
the cross-panel constraint that the single $\Delta t$ of \eqref{eq:dk}
imposes. $C(\pi)$ therefore does not reduce to a linear assignment, and the
labels are returned by a deterministic two-tier search rather than by a
global minimization of $C$ under the exact criterion. Let $C^{\rm cb}$ be \eqref{eq:assoc_perm} with
the near-field term of \eqref{eq:assoc_score} evaluated on the cached coarse
codebook, and let $\pi_{(1)},\pi_{(2)}$ be its two best permutations among
all $K!$. The second tier re-scores only the column--panel pairs those two
contain, with the exact spherical projection, and returns
\begin{equation}
	\hat\pi=\arg\min_{\pi\in\{\pi_{(1)},\pi_{(2)}\}}C(\pi),
	\qquad
	m_{\rm assoc}=\big|C(\pi_{(1)})-C(\pi_{(2)})\big|
	\label{eq:assoc_two_tier}
\end{equation}
with $m_{\rm assoc}$ the reported margin (supplement). The first tier is the one part of the estimator whose
cost grows factorially in $K$, negligible at $K=3$ but a real limit on panel
count. The front end therefore outputs $K$ delay--factor triples
$\{\hat\tau_j,\tilde\ba_j,\tilde\bt_j\}$ carrying physical panel labels
$\hat\pi$, the input to the geometric acquisition below.

\subsection{Beam-Space Acquisition and Common-Geometry Initialization}
\label{subsec:beamspace}

\textit{1) Panel-local beam-space acquisition:}
By \eqref{eq:factor_meaning} the factor $\tilde\bt_k$ that $\hat\pi$ assigns
to panel $k$ is $\bh_k$ up to an unknown scale, so the geometry it carries is
read back out one panel at a time, before the $K$ readings are reconciled
into a single position, by the scale-invariant fit
\begin{equation}
 J_{\rm RIS}(\boldsymbol\eta)
 =\|\bPi_{\bh_k(\boldsymbol\eta)}^\perp\tilde\bt_k\|^2
 =\|\tilde\bt_k\|^2-
 \frac{|\bh_k^{\mathsf H}(\boldsymbol\eta)\tilde\bt_k|^2}
 {\|\bh_k(\boldsymbol\eta)\|^2},
 \label{eq:Jris_short}
\end{equation}
with $\bh_k(\boldsymbol\eta)=\bOmega_k[\ba_{{\rm RB},k}\odot
\ba^{\rm NF}(\boldsymbol\eta)]$ over the panel-local coordinates
$\boldsymbol\eta$ defined above. Because a fixed-count angular dictionary of
the polar or beam-training kind~\cite{cui2022channel,zheng2025wideband} has a
step set by the search box rather than by the panel resolution
$\lambda_c/D$, the dominant angular search is moved instead to the element
domain. The training code enters $\bh_k$ only on the left, so moving
$\bOmega_k^{\mathsf H}$ across and then splitting the Hadamard product gives
\begin{align}
 \bh_k^{\mathsf H}(\boldsymbol\eta)\tilde\bt_k
 &=\big[\ba_{{\rm RB},k}\odot\ba^{\rm NF}(\boldsymbol\eta)\big]^{\mathsf H}
 \big(\bOmega_k^{\mathsf H}\tilde\bt_k\big)\nonumber\\
 &=\big[\ba^{\rm NF}(\boldsymbol\eta)\big]^{\mathsf H}\tilde\bg_k,
 \quad
 \tilde\bg_k=\ba_{{\rm RB},k}^{\ast}\odot
 (\bOmega_k^{\mathsf H}\tilde\bt_k).
 \label{eq:adjoint_short}
\end{align}
One adjoint pass therefore fixes $\tilde\bg_k$ per panel and leaves
$\ba^{\rm NF}(\boldsymbol\eta)$ as the only $\boldsymbol\eta$-dependent
factor in the numerator of \eqref{eq:Jris_short}. The denominator
$\|\bOmega_k[\ba_{{\rm RB},k}\odot\ba^{\rm NF}(\boldsymbol\eta)]\|^2$ admits
no such reduction and is retained in the exact evaluation. Expanding \eqref{eq:delta} to second order about
the panel center separates the residual phase into a part linear in the
direction cosines $\bu$ and a curvature part set by the trial range $\tilde d$,
\begin{equation}
	\delta_{k,m}\simeq-\brho_{k,m}^{\mathsf T}\bu
	+\frac{\|\brho_{k,m}\|^2-(\brho_{k,m}^{\mathsf T}\bu)^2}{2\tilde d},
	\label{eq:fresnel}
\end{equation}
so the paraxial dechirp
$\tilde\bg_{k,\tilde d}[m]=\tilde\bg_k[m]e^{j\kappa\|\brho_{k,m}\|^2/(2\tilde d)}$
removes the isotropic quadratic term and leaves the plane-wave phase
$e^{j\kappa\brho_{k,m}^{\mathsf T}\bu}$ as the dominant angular dependence,
together with a residual direction-dependent curvature in
$(\brho_{k,m}^{\mathsf T}\bu)^2/(2\tilde d)$. One zero-padded two-dimensional
discrete Fourier transform (2-D DFT) then scores every direction cosine at
once by numerator magnitude, on a grid that tracks the Rayleigh limit at the
cost of one transform per trial range, with no dictionary formed or scanned
atom by atom. Because of that residual, because
the transform sees the numerator alone, and because range resolves more
weakly than direction, \eqref{eq:fresnel} only proposes: each visible angular
peak receives an exact spherical range scan and is re-scored by the full
\eqref{eq:Jris_short} before local refinement, so the approximation never
replaces the spherical model.

Whatever the candidate set, every candidate is scored by the same exact
criterion \eqref{eq:Jris_short}, so the candidate set changes acquisition
coverage and cost but not the forward model; the set used by each experiment
is stated in Section~\ref{subsec:res_setup}.

\textit{2) Common-geometry fusion:}
Each panel-local fix $\hat{\boldsymbol\eta}_k$ maps through the calibrated
panel pose back to a global position
$\hat\bp_k^{\rm loc}=\br_k+\bR_k^{\mathsf T}\bxi_k(\hat{\boldsymbol\eta}_k)$,
inverting $\bxi_k(\bp)=\bR_k(\bp-\br_k)$. These $K$ positions generally
disagree; the common-geometry stage replaces them by the single position that
best explains all $K$ training factors at once, each up to its own unknown
complex scale,
\begin{align}
 J_{\rm geom}(\bp)
 &=\min_{\balpha\in\C^K}\sum_{k=1}^K \varpi_k
 \|\tilde\bt_k-\alpha_k\bh_k(\bp)\|^2
 \label{eq:Jgeom_short}\\
 &=\sum_{k=1}^K \varpi_k\left[\|\tilde\bt_k\|^2-
 \frac{|\bh_k^{\mathsf H}(\bp)\tilde\bt_k|^2}{\|\bh_k(\bp)\|^2}\right],
 \nonumber
\end{align}
the second line following by substituting the per-panel profiled scale
$\hat\alpha_k(\bp)=\bh_k^{\mathsf H}(\bp)\tilde\bt_k/\|\bh_k(\bp)\|^2$. Only
the component of $\tilde\bt_k$ orthogonal to $\coll(\bh_k)$ survives, so
$J_{\rm geom}$ is blind to the unknown path gains. The
weights $\varpi_k=\max\{e^{-\varrho_k/\varrho_0},\varpi_{\min}\}$ down-weight a column
whose rank-one split is poor while keeping every panel in the fit. We minimize
\eqref{eq:Jgeom_short} with bounded L-BFGS-B~\cite{byrd1995lbfgsb} from a
small deterministic set of starts built from the clipped coordinate-wise
median
$\hat\bp_0=\clip_{\mathcal P}\{\median_k\hat\bp_k^{\rm loc}\}$; the
constants are supplementary.

\textit{3) Jones-anchor refresh:}
At the selected $\hat\bp_{\rm GI}$, the $\lambda_k=0$ instance of the
certified clock profiler of Section~\ref{subsec:ccop} refreshes the Jones
directions at a fixed position,
\begin{equation}
 (\hat{\Delta t}_{\rm ref},\hat\bx_{\rm ref})=
 \arg\min_{\Delta t\in\mathcal T,\bx}
 \|\by-\bA(\hat\bp_{\rm GI},\Delta t)\bx\|^2.
 \label{eq:refresh_short}
\end{equation}
With $\hat\bx_{{\rm ref},k}$ the $k$th two-entry block of $\hat\bx_{\rm ref}$,
the anchor is $\hat\be_k=\hat\bx_{{\rm ref},k}/\|\hat\bx_{{\rm ref},k}\|_2$,
obtained at the common position from a conditional clock solve that carries
the same optimality-gap certificate. Phase~I therefore outputs
$\hat\bp_{\rm GI}$ and the anchors $\{\hat\be_k\}$, which initialize the
Phase-II directional regularizer.

\subsection{Common-Clock Orbit-Profiled Jones Variable Projection}
\label{subsec:ccop}

\textit{1) Unitary clock orbit and directional Jones regularization:}
Phase~II returns from the basin just built to the exact forward model
underlying \eqref{eq:ml_problem}, augmented by a directional Jones penalty.
Its $2K$ Jones coefficients enter linearly and are eliminated in
closed form by variable projection~\cite{golub1973varpro}, leaving four
nonlinear coordinates. By Theorem~\ref{thm:compress} the criterion may be
evaluated on the compressed observation of Phase~I without approximation,
which is what the implementation does. The common clock is the only nonlinear
coordinate along which the likelihood admits an exact unitary symmetry. Let
$\bA_0(\bp)\triangleq\bA(\bp,0)$ and
$\bD_{\rm clk}(\Delta t)=\diag([e^{-j2\pi(n-\frac{N-1}{2})\Delta f\Delta t}]_{n=0}^{N-1})$.
Then
\begin{equation}
 \bU_{\rm clk}(\Delta t)=\bI_T\otimes\bD_{\rm clk}(\Delta t)\otimes\bI_I,
 \quad \bA(\bp,\Delta t)=\bU_{\rm clk}(\Delta t)\bA_0(\bp),
 \label{eq:orbit_short}
\end{equation}
and unitarity makes the Jones Gram clock invariant,
$\bA^{\mathsf H}\bA=\bA_0^{\mathsf H}\bA_0\triangleq\bG_0(\bp)$.
Phase~II uses a Gram-scaled block-diagonal penalty
\begin{equation}
 \bR_{\rm dir}(\bp)=\blkdiag_k\left[
 \lambda_k\tfrac12\tr\bG_{0,kk}(\bp)\,\bPi_{\hat\be_k}^{\perp}\right],
 \label{eq:Rdir_short}
\end{equation}
which penalizes only the component of $\bx_k$ orthogonal to the refreshed
anchor $\hat\be_k$; in particular $\bR_{{\rm dir},kk}\hat\be_k=\mathbf0$, so
the anchor direction is left free while the gain $\beta_k$ remains an
unconstrained nuisance, which a penalty on
$\|\bx_k-\hat\bx_{{\rm ref},k}\|$ would not achieve. What the penalty must
repair is set entirely by the receiver selection. Each Kronecker factor of
\eqref{eq:Ak} contributes its own Gram, and $\bd_k$ and
$\bv_{{\rm B},k}$ have unit-modulus entries, so
\begin{equation}
	\bG_{0,kk}=N M_A\|\bh_k(\bp)\|^2\,
	[\bTheta_k^{({\rm mode})}]^{\mathsf H}\bTheta_k^{({\rm mode})}.
	\label{eq:g0block}
\end{equation}
Full-EVS reception gives
$[\bTheta_k^{(6)}]^{\mathsf H}\bTheta_k^{(6)}=2\bI_2$ by \eqref{eq:Theta},
hence $\bG_{0,kk}\succ\mathbf0$ and every Jones coordinate of panel $k$ is
seen by the data; a mask with $d_{\rm evs}<2$ leaves
$\bS_{\rm mode}\bTheta_k^{(6)}$ of rank one, so $\bG_{0,kk}$ is singular and
one Jones coordinate per panel is invisible. Supported on the complement of
$\hat\be_k$, $\bR_{\rm dir}$ fixes that coordinate at the Phase-I anchor
without displacing the identifiable one, which is how it secures the
$\bG_\lambda\succ\mathbf0$ hypothesis of Theorem~\ref{thm:clock}. The
dimensionless weight is $\lambda_k=1$ throughout, so the penalty enters at
the scale of the per-path data Gram (Remark~\ref{rem:peb_reading}); how far
it is from active at the returned solution is measured in
Section~\ref{subsec:res_c1}.

\textit{2) Jones variable projection and the trigonometric clock profile:}
Phase~II accordingly minimizes the penalized exact-forward-model criterion
\begin{equation}
 J(\bp,\Delta t,\bx)=\|\by-\bA(\bp,\Delta t)\bx\|_2^2
 +\bx^{\mathsf H}\bR_{\rm dir}(\bp)\bx,
 \label{eq:penalized_short}
\end{equation}
whose first term is the nominal criterion \eqref{eq:ml_problem}. It is
\eqref{eq:penalized_short}, not \eqref{eq:ml_problem}, that
Theorem~\ref{thm:clock} nests and certifies; the two coincide at
$\lambda_k=0$, and Section~\ref{subsec:res_c1} reports the $\lambda$ sweep.
Substituting \eqref{eq:orbit_short} into \eqref{eq:penalized_short} and using
$\bU_{\rm clk}^{\mathsf H}\bU_{\rm clk}=\bI$ makes $J$ a quadratic in $\bx$
whose Hessian carries no clock dependence,
\begin{equation}
 J=\|\by\|_2^2-2\Real\{\bx^{\mathsf H}\bb(\bp,\Delta t)\}
 +\bx^{\mathsf H}\bG_\lambda(\bp)\bx,
 \label{eq:jquad}
\end{equation}
with $\bG_\lambda=\bG_0+\bR_{\rm dir}$, which reduces to $\bG_0$ at
$\lambda_k=0$, and $\bb=\bA_0^{\mathsf H}\bU_{\rm clk}^{\mathsf H}\by$. The
Wirtinger stationarity condition
$\partial J/\partial\bx^{\ast}=\bG_\lambda\bx-\bb=\mathbf0$ hence removes all
$2K$ Jones coefficients in one step,
\begin{align}
 \hat\bx&=\bG_\lambda^{-1}\bb,
 \quad Q(\bp,\Delta t)=\bb^{\mathsf H}\bG_\lambda^{-1}\bb,\nonumber\\
 F(\bp,\Delta t)&\triangleq\min_{\bx\in\C^{2K}}J=\|\by\|_2^2-Q,
 \label{eq:vp_short}
\end{align}
leaving the 3-D outer objective
\begin{equation}
 \widetilde F(\bp)=\|\by\|^2-
 \max_{\Delta t\in\mathcal T}Q(\bp,\Delta t).
 \label{eq:nested_short}
\end{equation}
Grouping the sufficient-statistic correlations by tone as
$\bb(\bp,\Delta t)=\sum_{n=0}^{N-1}\bu_n(\bp)
e^{j2\pi(n-\frac{N-1}{2})\Delta f\Delta t}$ and carrying them through the
clock-independent $\bG_\lambda^{-1}$,
\begin{align}
 Q(\bp,\Delta t)
 &=\sum_{n=0}^{N-1}\sum_{n'=0}^{N-1}
 \bu_n^{\mathsf H}\bG_\lambda^{-1}\bu_{n'}\,
 e^{j2\pi(n'-n)\Delta f\Delta t}\nonumber\\
 &=\sum_{\ell=-(N-1)}^{N-1}\gamma_\ell(\bp)
 e^{j2\pi\ell\Delta f\Delta t},\quad \gamma_{-\ell}=\gamma_\ell^\ast,
 \label{eq:trig_short}
\end{align}
with $\gamma_\ell(\bp)=\sum_{n'-n=\ell}\bu_n^{\mathsf H}(\bp)
\bG_\lambda^{-1}(\bp)\bu_{n'}(\bp)$: the clock survives only through tone
differences, so at fixed $\bp$ the profile $Q$ is a real trigonometric
polynomial of degree at most $N-1$ and one factorization of $\bG_\lambda$
with one zero-padded FFT supplies all initial clock candidates.
Differentiating \eqref{eq:trig_short} twice gives
$Q''=-(2\pi\Delta f)^2\sum_\ell\ell^2\gamma_\ell
e^{j2\pi\ell\Delta f\Delta t}$, so the curvature is bounded uniformly on
$\mathcal T$ by $L_2\triangleq(2\pi\Delta f)^2\sum_\ell\ell^2|\gamma_\ell|$
and second-order Taylor expansion about the center $t_c$ of an interval of
half-width $r_t$ yields the branch bound
\begin{equation}
 Q(t)\le Q(t_c)+|Q'(t_c)|r_t+\tfrac12L_2r_t^2.
 \label{eq:bound_short}
\end{equation}
\textit{3) Certified clock maximization and outer refinement:}
Branch-and-bound maintains \eqref{eq:bound_short} over $\mathcal T$ and
returns an incumbent with
$Q(\Delta t^\star)-Q(\hat{\Delta t})\le U-Q(\hat{\Delta t})
\triangleq g_{\rm cert}$, where $U$ is the largest surviving interval upper
bound; subdivision stops once $g_{\rm cert}\le\epsilon_{\rm clk}$ or a cap
on subdivisions is reached. Every outer position therefore carries a
deterministic gap certificate, $\epsilon_{\rm clk}$-optimal whenever
termination is by tolerance, as Theorem~\ref{thm:clock}(iii) states. The outer bounded search starts from $\hat\bp_{\rm GI}$ and, where the
maximizing branch is unique and interior, uses the envelope-theorem gradient
\begin{equation}
	\nabla_{\bp}\widetilde F(\bp)
	=-\nabla_{\bp}Q(\bp,\Delta t)\big|_{\Delta t=\hat{\Delta t}(\bp)},
	\label{eq:envelope}
\end{equation}
with a derivative-free safeguard~\cite{powell1964direct} otherwise. Phase~II outputs
$(\hat\bp,\widehat{\Delta t},\hat\bx)$ together with the certificate attached
to every conditional clock solve it evaluated. Tolerances, the safeguard
trigger and pseudocode for both phases are supplementary.


\section{Structural Analysis and Guarantees}
\label{sec:analysis}

This section formalizes C1--C3 under the conditions stated with each result.
After the free-Jones reference information and its local rank condition, the
three guarantees follow in turn from the Kronecker projector structure of
\eqref{eq:premise}, from its calibrated receive subspace, and from its
common-clock unitary orbit.

\textit{A. Reference information and identifiability:}
Let $\bD_\chi=[\partial(\bA\bx)/\partial\chi_i]_{i=1}^4$. Eliminating the
unknown complex Jones coefficients from the white-Gaussian Fisher
information matrix gives the free-Jones equivalent Fisher information
matrix (EFIM), the reference information of the unpenalized data model,
\begin{equation}
 \bJ_\chi^{\rm free}=\frac{2}{\sigma^2}
 \Real\{\bD_\chi^{\mathsf H}\bPi_{\bA}^{\perp}\bD_\chi\}.
 \label{eq:efim_short}
\end{equation}
Local position--clock identifiability after eliminating the Jones nuisance
requires
\begin{equation}
 \rank_{\R}[\Real\{\bPi_{\bA}^{\perp}\bD_\chi\}^{\mathsf T},
 \Imag\{\bPi_{\bA}^{\perp}\bD_\chi\}^{\mathsf T}]^{\mathsf T}=4,
 \label{eq:rankcond}
\end{equation}
equivalently $\bJ_\chi^{\rm free}\succ\mathbf0$, that matrix being the Gram
of the real stacking. It then yields the position and clock error bounds
(PEB, CEB) ${\rm PEB}=\sqrt{\tr[(\bJ_p^{\rm eff})^{-1}]}$ and
${\rm CEB}=\sqrt{[(\bJ_\chi^{\rm free})^{-1}]_{4,4}}$, with
$\bJ_p^{\rm eff}=\bJ_{pp}-\bJ_{pt}\bJ_{tt}^{-1}\bJ_{tp}$. The delay-only
Jacobian of \eqref{eq:dk} is
\begin{equation}
	\bD_\tau(\bp)
	=\frac{\partial[\tau_1,\ldots,\tau_K]^{\mathsf T}}
	{\partial\bchi^{\mathsf T}}
	=\begin{bmatrix}
	c_0^{-1}\bs_{{\rm UR},1}^{\mathsf T}\bR_1 & 1\\
	\vdots & \vdots\\
	c_0^{-1}\bs_{{\rm UR},K}^{\mathsf T}\bR_K & 1
	\end{bmatrix}\in\R^{K\times4},
	\label{eq:delayjac}
\end{equation}
whose all-ones clock column is what row differencing removes. At $K=3$,
$\rank\bD_\tau\le3<4$, so the cascaded delays cannot identify $\bchi$ by
themselves; the near-field aperture supplies the missing direction, because
after complex-scale elimination the projected tangents
$\bPi_{\bh_k}^{\perp}[\partial\bh_k/\partial x,\partial\bh_k/\partial y,
\partial\bh_k/\partial z]$ retain the phase slope and curvature of
\eqref{eq:delta}. Three limits apply: the test is local; gains and Jones
directions are unknown deterministic nuisances, the pseudoinverse projectors
covering the rank-deficient Jones blocks a scalar or dual mask produces; and
global panel-permutation and finite-bandwidth ambiguities lie outside it.
The derivation of \eqref{eq:efim_short} and of the real-rank equivalence is
supplementary.

\textit{B. C1---Maxwell--Jones information splitting:}
Both parts of C1 rest on one identity: because $\bA_k$ is a Kronecker
product, so is its projector, and the complement separates into a
polarization-orthogonal component along the geometric factor and a
geometry-orthogonal component acting identically on every polarization.

\begin{lemma}[Kronecker projector splitting]
\label{lem:kron}
Let $\bc\neq\mathbf0$ and $\bTheta\neq\mathbf0$ be arbitrary, with
$\bP_{\bTheta}=\bTheta\bTheta^{\dagger}$. With $\bA=\bc\otimes\bTheta$,
\begin{equation}
 \bPi_{\bA}^{\perp}
 =\bP_{\bc}\otimes\bPi_{\bTheta}^{\perp}+\bPi_{\bc}^{\perp}\otimes\bI,
 \qquad \bP_{\bc}=\frac{\bc\bc^{\mathsf H}}{\|\bc\|^2}.
 \label{eq:kronsplit}
\end{equation}
\end{lemma}

\begin{IEEEproof}
$\bA^{\mathsf H}\bA=(\bc^{\mathsf H}\bc)(\bTheta^{\mathsf H}\bTheta)$ and
$\bA^{\dagger}=\bc^{\dagger}\otimes\bTheta^{\dagger}$, so
$\bP_{\bA}=\bP_{\bc}\otimes\bP_{\bTheta}$. Expanding
$\bI=(\bP_{\bc}+\bPi_{\bc}^{\perp})\otimes(\bP_{\bTheta}
+\bPi_{\bTheta}^{\perp})$ and subtracting $\bP_{\bA}$ gives
\eqref{eq:kronsplit}. No rank assumption is used, so the identity survives
the component mask of C1(ii), which can make the retained Jones block rank
deficient.
\end{IEEEproof}

\begin{theorem}[C1: Maxwell--Jones information splitting]
\label{thm:split}
\textbf{(i) Information consequence.} For one panel, write
$\bA=\bc\otimes\bTheta$, $\boldsymbol{\zeta}=\bTheta\bx$,
$\dot\bc_i=\partial\bc/\partial\chi_i$, and
$\dot{\boldsymbol{\zeta}}_i=(\partial\bTheta/\partial\chi_i)\bx$. Then
\begin{equation}
 [\bD_\chi^{\mathsf H}\bPi_{\bA}^{\perp}\bD_\chi]_{ij}
 =\|\boldsymbol{\zeta}\|^2\dot\bc_i^{\mathsf H}\bPi_{\bc}^\perp\dot\bc_j
 +\|\bc\|^2\dot{\boldsymbol{\zeta}}_i^{\mathsf H}
 \bPi_{\bTheta}^\perp\dot{\boldsymbol{\zeta}}_j.
 \label{eq:split_short}
\end{equation}
Since calibrated infrastructure gives
$\partial\bTheta/\partial\bchi=\mathbf0$, the second term vanishes.
Throughout C1, $\bTheta=\bTheta_k^{(6)}$ is the \emph{full} block of
\eqref{eq:Theta} and $\bS_{\rm mode}$ one of the selections
\eqref{eq:smode}, so for a single panel with nonzero retained signal
\begin{equation}
	\bJ_\chi^{\bS_{\rm mode}}=\alpha_{\bS_{\rm mode}}\bJ_\chi^{\rm full},
	\quad
	{\rm PEB}^{\bS_{\rm mode}}
	=\alpha_{\bS_{\rm mode}}^{-1/2}\,{\rm PEB}^{\rm full},
	\label{eq:c1_scaling}
\end{equation}
with $\alpha_{\bS_{\rm mode}}
=\|\bS_{\rm mode}\bTheta\bx\|^2/\|\bTheta\bx\|^2$, the ${\rm PEB}$ identity
requiring in addition $\bJ_\chi^{\rm full}\succ\mathbf0$ so that both bounds
are finite.

\textbf{(ii) Discrimination consequence.} Define
$\bA_k^{(\bS)}(\bchi)=\bc_k(\bchi)\otimes
\bS_{\rm mode}\bTheta_k^{(6)}$. If a competitor block matches panel $k$'s
space--frequency signature, i.e.
$\bc_{k'}(\bchi')\parallel\bc_k(\bchi)$, its free-Jones residual is
\begin{equation}
 \|\bPi_{\bA_{k'}^{(\bS)}(\bchi')}^\perp
 \bA_k^{(\bS)}(\bchi)\bx_k\|
 =\|\bc_k\|\,\|\bPi_{\bS_{\rm mode}\bTheta_{k'}^{(6)}}^\perp
 \bS_{\rm mode}\bTheta_k^{(6)}\bx_k\|.
 \label{eq:panelres_short}
\end{equation}
If the retained competitor block
$\bS_{\rm mode}\bTheta_{k'}^{(6)}\in\C^{d_{\rm evs}\times2}$ has full row rank
for $d_{\rm evs}\le2$, this residual is zero. With all six components it is at least
$\|\bc_k\|\|\bTheta_k^{(6)}\bx_k\|\sin\theta_{\min}$, where
$\theta_{\min}$ is the minimum principal angle between the two panel
Maxwell subspaces.
\end{theorem}

\begin{IEEEproof}
(i) A Jacobian column of $\bA\bx=\bc\otimes\boldsymbol{\zeta}$ is
$\bD_i=\dot\bc_i\otimes\boldsymbol{\zeta}
+\bc\otimes\dot{\boldsymbol{\zeta}}_i$. Insert Lemma~\ref{lem:kron} into
$\bD_i^{\mathsf H}\bPi_{\bA}^{\perp}\bD_j$ and apply the mixed-product
rule: every cross term carries a factor $\bPi_{\bc}^{\perp}\bc=\mathbf0$ or
$\bPi_{\bTheta}^{\perp}\boldsymbol{\zeta}=\mathbf0$, leaving the two terms
of \eqref{eq:split_short}. A mask replaces $\bTheta$ by
$\bS_{\rm mode}\bTheta$ and leaves $\bc$ untouched, so with
$\dot{\boldsymbol{\zeta}}_i=\mathbf0$ the whole matrix scales by
$\alpha_{\bS_{\rm mode}}$.

(ii) Apply \eqref{eq:kronsplit} to the competitor
$\bA_{k'}^{(\bS)}(\bchi')$ and evaluate it at
$\bc_k\otimes\bS_{\rm mode}\bTheta_k^{(6)}\bx_k$. Under
$\bc_{k'}\parallel\bc_k$ the term $\bPi_{\bc_{k'}}^{\perp}\otimes\bI$
annihilates its argument, leaving
$\bP_{\bc_{k'}}\otimes\bPi^{\perp}_{\bS_{\rm mode}\bTheta_{k'}^{(6)}}$ and
hence \eqref{eq:panelres_short}. If
$\bS_{\rm mode}\bTheta_{k'}^{(6)}\in\C^{d_{\rm evs}\times2}$ has row rank
$d_{\rm evs}\le2$ its range is all of $\C^{d_{\rm evs}}$ and that projector is
zero. For $d_{\rm evs}=6$ the two Maxwell subspaces are proper subspaces of
$\C^6$ separated by a minimum principal angle $\theta_{\min}>0$, so
$\|\bPi_{\bTheta_{k'}^{(6)}}^{\perp}\bz\|\ge\sin\theta_{\min}\|\bz\|$ for
every $\bz\in\coll(\bTheta_k^{(6)})$, which is the stated bound.
\end{IEEEproof}

C1 says that polarization enters the geometry as a scalar: by
\eqref{eq:c1_scaling} component selection cannot rotate a panel's
information eigenvectors, only rescale them by the polarization energy it
retains. That scaling is single-panel: $\bPi_{\bA}^{\perp}$ built on the full
$\bA=[\bA_1,\ldots,\bA_K]$ factors into per-panel projectors only when the
blocks and their projected tangents are mutually orthogonal, and
$\alpha_{\bS_{\rm mode}}$ differs from panel to panel, so a receiver choice
reweights panels unevenly rather than rescaling the aggregate. Part (ii) is
a statement of a different kind: a wrong-panel hypothesis distance rather
than a local perturbation, which vanishes for $d_{\rm evs}\le2$ under its
full-row-rank condition while the information of (i) merely rescales. Local
information and cross-panel discrimination are therefore separate
consequences of receiver choice, and only the first is visible to a local
bound.

\begin{remark}[Scope of the free-Jones reference bound]
\label{rem:peb_reading}
The frozen weight $\lambda_k=1$ does not taper with $\sigma$, so the
free-Jones PEB is a reference for the unpenalized data model and is not
claimed as a proven bound for the penalized estimator; the $\lambda$-sweep of
Section~\ref{subsec:res_c1} measures the gap directly.
\end{remark}

\textit{C. C2---Sufficient Maxwell--Kronecker compression:}
\begin{theorem}[C2: Sufficient Maxwell--Kronecker compression]
\label{thm:compress}
Let $\bU_S=\bI_{NT}\otimes\bQ_S$ be the semi-unitary lifting matrix in the
vectorization of \eqref{eq:obs}, and suppose
$\coll[\bA(\bchi)]\subseteq\coll(\bU_S)$ for all admissible $\bchi$. Write
$\by_c=\bU_S^{\mathsf H}\by$ and
$c_\perp=\|(\bI-\bU_S\bU_S^{\mathsf H})\by\|^2$. Under the matched
white-Gaussian model:
(i) for known $\sigma^2$, $\by_c$ is sufficient for $(\bchi,\bx)$;
(ii) for unknown $\sigma^2$, the pair $(\by_c,c_\perp)$ is sufficient for
$(\bchi,\bx,\sigma^2)$, and $\by_c$ alone still preserves the maximizer in
$(\bchi,\bx)$ of the $\sigma^2$-profiled likelihood, because
\begin{equation}
 \|\by-\bA(\bchi)\bx\|^2
 =\|\by_c-\bU_S^{\mathsf H}\bA(\bchi)\bx\|^2+c_\perp
 \label{eq:isometry_short}
\end{equation}
and $c_\perp$ does not depend on $(\bchi,\bx)$;
(iii) $\by_c$ alone preserves the profiled optimizer, the derivatives of
$\|\by-\bA(\bchi)\bx\|^2$ in $(\bchi,\bx)$ to every order, and the free-Jones
EFIM \eqref{eq:efim_short} exactly;
(iv) the noiseless delay poles are preserved provided the compressed
non-frequency snapshot matrix $[\bw_1,\ldots,\bw_K]$ of
\eqref{eq:freqsnap}, with $\bw_k=\ba_k\otimes\bh_k$ and
$\ba_k=\beta_k\bQ_S^{\mathsf H}\bBm_k\be_k$, has rank $K$.
\end{theorem}

\begin{IEEEproof}
Complete $\bU_S$ to a unitary $[\bU_S,\bU_S^{\perp}]$. The subspace
inclusion makes $(\bU_S^{\perp})^{\mathsf H}\by\sim
\mathcal{CN}(\mathbf0,\sigma^2\bI)$, independent of $\bU_S^{\mathsf H}\by$
and free of $(\bchi,\bx)$, which gives (i); for unknown $\sigma^2$ it
involves the discarded block only through the scalar $c_\perp$, so the
factorization theorem gives (ii). Because $\bA=\bU_S\bU_S^{\mathsf H}\bA$
the cross term in the orthogonal decomposition of $\by$ vanishes, which is
\eqref{eq:isometry_short} with a data-only second term. Writing
$\bA=\bU_S\bA_c$ and $\bD_\chi=\bU_S\bD_{\chi,c}$, semi-unitarity gives
$\bPi_{\bA}^{\perp}\bD_\chi=\bU_S\bPi_{\bA_c}^{\perp}\bD_{\chi,c}$,
pseudoinverses included; taking Grams leaves \eqref{eq:efim_short}
unchanged, proving (iii). For (iv), $\bQ_S^{\mathsf H}$ acts on the EVS mode
alone and commutes with both Vandermonde frequency factors, so the noiseless
pole set is unchanged and shift-invariance identifiability survives whenever
the compressed snapshot matrix has column rank $K$.
\end{IEEEproof}

C2 separates statistical sufficiency from finite-sample algorithmic
conditioning. Equation~\eqref{eq:isometry_short} is a pointwise identity,
not an approximation, and its discarded term carries no dependence on
$(\bchi,\bx)$, so no candidate changes its score relative to any other.
Phase~I is nonetheless an ESPRIT-based subspace procedure rather than the
exact ML optimizer, so removing the $I-r$ noise-only receive directions can
materially improve its finite-sample recovery without contradicting the
isometry. Pole preservation additionally requires
Theorem~\ref{thm:compress}(iv). That condition holds generically once
$rT\ge K$ under nondegenerate panel responses and training, but genericity
is a measure-zero statement rather than an exhaustive classification: the
rank can still fail under degeneracies such as an annihilated path, a zero
gain, or systematically dependent panel signatures. The delay-subspace
separation is therefore reported as a measured per-trial diagnostic in
Section~\ref{subsec:res_c2} rather than assumed.

\textit{D. C3---Certified common-clock profiling:}
\begin{theorem}[C3: Certified common-clock profiling]
\label{thm:clock}
Let $\mathcal P$ and $\mathcal T$ be the compact search sets of
Section~\ref{subsec:defchain} and assume
$\bG_\lambda(\bp)=\bG_0(\bp)+\bR_{\rm dir}(\bp)\succ\mathbf0$ on $\mathcal P$, which
for $d_{\rm evs}<2$ requires $\lambda_k>0$.

\textbf{(i) Exact nesting.} Analytic Jones elimination and global inner
clock maximization nest the penalized criterion \eqref{eq:penalized_short}
exactly:
$F(\bp,\Delta t)=\min_{\bx}J(\bp,\Delta t,\bx)=\|\by\|^2-Q(\bp,\Delta t)$,
\begin{equation}
 \min_{\bp\in\mathcal P}\widetilde F(\bp)
 =\min_{\bp\in\mathcal P,\,\Delta t\in\mathcal T,\,\bx\in\C^{2K}}
 J(\bp,\Delta t,\bx),
 \label{eq:nestval_short}
\end{equation}
and the argument sets correspond: pairing any outer minimizer $\bp^\star$
with a global inner clock maximizer $\Delta t^\star$ of $Q(\bp^\star,\cdot)$
and with
$\bx^\star=\bG_\lambda^{-1}(\bp^\star)\bb(\bp^\star,\Delta t^\star)$ recovers
a joint minimizer.

\textbf{(ii) Unit invariance.} The outer profile is invariant under every
invertible affine clock-unit reparameterization with $\mathcal T$
transformed accordingly.

\textbf{(iii) Certified clock solution.} The interval bound
\eqref{eq:bound_short} guarantees at every visited position
\begin{equation}
 Q(\Delta t^\star)-Q(\hat{\Delta t})\le g_{\rm cert}
 =U-Q(\hat{\Delta t}),
 \label{eq:cert_short}
\end{equation}
and $g_{\rm cert}\le\epsilon_{\rm clk}$ whenever termination occurs by the
prescribed tolerance rather than the subdivision cap.
\end{theorem}

\begin{IEEEproof}
(i) $\bG_\lambda\succ\mathbf0$ makes \eqref{eq:jquad} a strictly convex
quadratic in $\bx$, so the inner minimum $\|\by\|^2-Q$ of
\eqref{eq:vp_short} is attained at every $(\bp,\Delta t)$; minimization may
then be performed in any order, which is \eqref{eq:nestval_short}, and the
correspondence of argument sets follows from the same attainment. (ii) An
invertible affine change of clock unit is a bijection of $\mathcal T$ onto
its image, so the attained value set of $Q(\bp,\cdot)$, hence
$\widetilde F$, is unchanged. (iii) The curvature bound $L_2$ derived from
\eqref{eq:trig_short} holds uniformly on $\mathcal T$, so
\eqref{eq:bound_short} is valid on every interval. The solver maintains a
finite cover of $\mathcal T$ and discards an interval only when its upper
bound falls below the incumbent, so no discarded interval can contain a
maximizer and the largest surviving bound obeys $Q(\Delta t^\star)\le U$.
This proves \eqref{eq:cert_short} under any stopping rule: the gap stays
valid even when the subdivision cap is reached, and only the tolerance test
depends on the cap.
\end{IEEEproof}

Theorem~\ref{thm:clock} globalizes only the conditional one-dimensional
clock solve and removes its outer coordinate; it does not certify the outer
three-dimensional position search, which remains local. The corresponding
clock-unit and certificate controls are reported in
Section~\ref{subsec:res_c3}.

\textit{E. Complexity:}
Phase~I is dominated by the $N\times rT$ delay-subspace step and the
factorial first tier of association. By Theorem~\ref{thm:compress}, Phase~II
evaluates the exact forward model on the sufficient compressed observation,
so compression cuts residual and Jacobian rows from $M=I\,N\,T$ to
$M_r=r\,N\,T$ without approximating the criterion. Each outer position
evaluation costs
$O(qM_r+q^2(N+T))+O(q^2N^2)+O(qM_c\log M_c+q^3)+C_{\rm cert}$, with $q=2K$,
$M_c$ the zero-padded clock-FFT length and $C_{\rm cert}$ the
branch-and-bound cost, which grows with the number of visited intervals.
Because the Kronecker structure of \eqref{eq:Ak} makes each Gram block a
cached position-independent Maxwell--Jones factor times one length-$N$ and
one length-$T$ inner product, the Gram update costs $O(q^2(N+T))$ rather
than $O(q^2M_r)$. Measured runtime and memory are reported in
Section~\ref{subsec:res_cost}.
\section{Numerical Results}
\label{sec:simulation}

\subsection{Protocol and Reference Bounds}
\label{subsec:res_setup}

Unless varied, the matched scene is
\begin{equation}
	\begin{aligned}
	(f_c,&K,M_R,M_A,N,\Delta f,T)\\
	&=(60~{\rm GHz},3,64^2,16,63,5~{\rm MHz},256),\\
	\bp^\star&=(1.25,0.55,0.75)~{\rm m},\quad\Delta t^\star=5~{\rm ns},
	\end{aligned}
	\label{eq:scene}
\end{equation}
with $\mathcal T=[0,10]$~ns and the panel poses, gains and Jones states
supplementary. The panel diagonal $D\simeq0.226$~m gives
$R_{\rm F}=2D^2/\lambda_c=20.5$~m, so
$d_{{\rm UR},k}\in[3.6,4.2]<R_{\rm F}<d_{{\rm RB},k}\in[28.6,31.2]$~m and the
neglected aperture taper stays below $3.2\%$: the one-sided model of
Section~\ref{sec:system_model} holds in this scene. Path gains and Jones
states are drawn independently per trial and
\begin{equation}
	{\rm SNR}=10\log_{10}\frac{\|\bmu_{\rm act}\|_2^2}
	{M_{\rm act}\sigma^2},
	\label{eq:snrdef}
\end{equation}
the pre-noise sample average over the active receive entries, absolute path
loss absorbed into this normalization. The receiver-mode study instead sets
$\sigma^2_{\rm scalar}=\sigma^2_{\rm dual}=\sigma^2_{\rm full}$ from the
full-EVS observation, comparing the masks at a common absolute noise level
rather than a common post-selection SNR.

With $e_{p,r}=\|\hat\bp_r-\bp^\star\|_2$ and
$e_{t,r}=|\widehat{\Delta t}_r-\Delta t^\star|$ over $R$ trials,
\begin{align}
	P_{\rm cat}^{(p)}&=\frac1R\sum_{r=1}^{R}
	\mathbf 1\{{\rm fail}_r\ \lor\ e_{p,r}>0.1~{\rm m}\},\nonumber\\
	P_{\rm cat}^{(t)}&=\frac1R\sum_{r=1}^{R}
	\mathbf 1\{{\rm fail}_r\ \lor\ e_{t,r}>1~{\rm ns}\},
	\label{eq:pcat}
\end{align}
and every conditional RMSE is taken over the complementary inlier set.
Phase~I acquires the basin on trial $r$ when $\hat\bp_{{\rm I},r}$ is finite
with $\|\hat\bp_{{\rm I},r}-\bp^\star\|_2\le0.1$~m. The declared operating
region is ${\rm SNR}\ge-10$~dB; results quoted at $-15$ or $-20$~dB are
below it and labeled as such.

Methods within a cell share the scene, RIS codes, Jones states, gains and
noise realization; trial counts are stated with each figure and table, and
rates from suites with independent seed roots are never pooled. Rate
intervals are exact $95\%$ Clopper--Pearson and paired catastrophic
comparisons use McNemar's exact test; all hyperparameters were fixed before
the reported cells were evaluated. Each fixed-SNR study is measured at
$-10$~dB except the model-order and RIS element-phase sweeps at $0$~dB.
Phase~I acquires from the beam-space proposals alone except in the
resolvability, position-generalization and resource-scaling sweeps, which
union them with the fixed-count dictionary and include panels down to
$16\times16$ elements (Section~\ref{subsec:beamspace}).

At $-10$~dB, over $20$ Jones/gain realizations at the default position, the
median free-Jones PEB and CEB are $0.404$~mm and $3.35$~ps. Oracle- and
free-Jones PEBs differ by under $0.005\%$, so the data-only bound is the
sole reference, and since the EFIM scales as $2/\sigma^2$ any bound quoted
at another SNR is this median scaled by $\sigma$.

\subsection{External Benchmark}
\label{subsec:res_benchmark}

\begin{figure}[!tb]
    \centering
    \includegraphics[width=\columnwidth]{fig_benchmark_main.pdf}
    \caption{External benchmark at the native read-outs, $960$
    shared-realization trials per SNR: the proposed MKSC-GI+CCOP-JVP, 4-D Jones-VP (4D-JVP) in range
    units, Algebraic CPD with joint mapping, VBI/SBL at its own closed-form
    fusion, and near-field OMP--SAGE--WLS. (a),~(c)~In-basin precision against the
    free-Jones PEB and the $\sigma$-scaled CEB reference of
    Section~\ref{subsec:res_setup}. (b),~(d)~Complementary CDFs at $-10$~dB; the
    dashed lines mark the $0.1$-m and $1$-ns catastrophic thresholds, so each
    crossing is that method's catastrophic rate. Catastrophic rates against SNR
    and the full campaign range are supplementary.}
    \label{fig:benchmark_combined}
\end{figure}

As shown in Fig.~\ref{fig:benchmark_combined}, the proposed estimator
matches the closest in-model control in precision and separates from it in
reliability. That control is a four-dimensional Jones-VP search (4D-JVP)
sharing the proposed forward model and carrying the clock in range units, so
the two differ only in acquisition. At $-10$~dB their median position errors
are $0.310$ and $0.312$~mm, statistically indistinguishable, while the
catastrophic rate falls from $1.98\%$ to $0.42\%$; from $-5$~dB upward the
proposed route has no catastrophic trial, which 4D-JVP reaches only at
$0$~dB. The advantage is therefore in basin acquisition rather than local
refinement, which is what C2 and Phase~I are designed to supply.

The three external routes are limited earlier, by floors that do not move
with SNR. At their native read-outs, variational Bayesian inference with
sparse Bayesian learning (VBI/SBL)~\cite{tipping2001sbl,li2024vbi} gives a
$26.2$-mm median at $9.79\%$, algebraic canonical polyadic decomposition
(CPD)~\cite{lin2021tensor,sidiropoulos2017tensor} is catastrophic on
$46.4\%$ of trials, and near-field orthogonal matching pursuit through
space-alternating generalized expectation--maximization and a weighted
least-squares read-out (OMP--SAGE--WLS)~\cite{pati1993omp,fleury1999sage,%
yan2025ris} on all of them at a $1.2$-m median; none improves from $-5$ to
$30$~dB.

A shared-refinement diagnostic, run on an independent seed root, locates
each floor by feeding every acquisition into the same exact-model
refinement. VBI/SBL then reaches $0.315$~mm at $0.52\%$ against $0.316$~mm
and $0.31\%$ for the proposed route, with McNemar not rejecting equality
($p=0.73$): its floor is a property of the adapted fusion, not of the
sparse Bayesian acquisition. The CPD route recovers comparable in-basin
precision ($0.438$ against $0.453$~mm conditional RMSE) but keeps a
$41.9\%$ catastrophic rate, so its loss is upstream of the read-out; on the
failing trials the tensor fit is as accurate as on the succeeding ones and
the panel association is correct in every case, which leaves the map from
converged CP factors to position as the remaining explanation.
OMP--SAGE--WLS is unchanged, the polish it runs already being the SAGE stage
its own reference prescribes, so its far-field direction acquisition is what
fails to bracket a basin in this blocked-LoS deep-near-field scene.

The clock results follow the same ordering. At $-10$~dB the
clock-catastrophic rate is $0.21\%$ for the proposed route against $1.56\%$
for 4D-JVP and $7.29\%$--$89.7\%$ for the three external routes; at $0$~dB
the proposed clock RMSE of $1.08$~ps sits at the $1.06$-ps CEB reference, a
descriptive comparison and not a bound test
(Remark~\ref{rem:peb_reading}), with 4D-JVP matching that inlier precision.
The ordering survives below the declared region, where at $-15$ and
$-20$~dB the proposed route keeps $1.25\%$ and $5.00\%$ against $6.88\%$ and
$22.08\%$ for 4D-JVP.

These numbers characterize the evaluated adaptations rather than the cited
methods: each external route is stopped at the continuous read-out its own
formulation prescribes, no comparator receives the proposed route's
exact-model Phase~II polish, and rates are paired only within one protocol.
McNemar's exact test on the paired catastrophic indicator rejects equality
against all four comparators ($p\le1.5\times10^{-3}$). Sources, grids, caps
and stopping rules are supplementary.

\begin{figure}[!tb]
\centering
\includegraphics[width=\columnwidth]{fig_receiver_main.pdf}
\caption{C1 information, $480$ paired trials per cell at a common absolute
noise level: matched conditional accuracy (solid) against the free-Jones PEB
(dashed), the modes ordering as Theorem~\ref{thm:split}(i) predicts. The
paired efficiency ratio and its bootstrap intervals are supplementary.}
\label{fig:c1_receiver}
\end{figure}

\subsection{C1: EVS Information and Panel Separability}
\label{subsec:res_c1}

\textit{Information.} Theorem~\ref{thm:split}(i) is an identity, so what
the receiver-mode experiment measures is the price of its scalar weight: at
$0$~dB the scalar, dual and full modes reach conditional RMSEs of $0.815$,
$0.330$ and $0.133$~mm, in the order the matched PEB predicts
(Fig.~\ref{fig:c1_receiver}), with \eqref{eq:c1_scaling} reproduced
numerically to $5.7\times10^{-16}$.

\textit{Separability.} Discrimination changes by more than information
alone predicts. At $-10$~dB the three modes carry catastrophic rates of
$14.17\%$, $2.29\%$ and $1.04\%$ -- a $13.6\times$ scalar/full ratio
against a $5.7\times$ matched-PEB ratio -- and the excess is the
panel-label loss of Theorem~\ref{thm:split}(ii).
Fig.~\ref{fig:c1_separability} isolates that mechanism on the same $K=3$
scene with the cascaded-delay separation and the Jones overlap of the
closest-delay panel pair set directly. Its resolution criterion (caption)
excludes position accuracy, so the curve measures separation of the
components and not the accuracy of the fix built on them. At $1$~ns
cascaded-delay separation, full-EVS resolution varies by $1.7$ percentage
points across the four tested Jones overlaps, below the $4.6$-point
Clopper--Pearson half-width of a single cell, so no dependence on overlap is
resolved; the dual-polarized and scalar modes instead collapse toward zero
as the overlap grows, spanning $82.7$ and $61.9$ points.
This is \eqref{eq:panelres_short} in the data: with $d_{\rm evs}\le2$ retained
components the competing Jones block spans $\C^{d}$ and the residual
separating the two panels can vanish outright, whereas six components hold
it above $\sin\theta_{\min}$. Delay separation limits every mode
alike; only the reduced modes are additionally limited by polarization.

\textit{Efficiency.} Inside the operating region the full-EVS RMSE/PEB
ratio is $0.982$--$0.992$ in the receiver suite and $1.017$--$1.020$ in the
independently seeded internal suite, and the dual-polarized ratio is
$1.057$--$1.084$. At $480$ trials the paired-bootstrap intervals are
$\pm6\%$ and $\pm9\%$ (supplement), so both modes are
consistent with efficiency while no ordering between them is resolved.
Their separation from scalar sensing in Fig.~\ref{fig:c1_receiver}
tracks the SNR-independent PEB ratios $5.7$ and $2.3$. The scalar mode's
high-SNR floor (near $0.098$~mm at $30$~dB) is receiver rather than
regularizer driven, and we attribute it to the rank-one structure of
single-component sensing. A penalty sweep over
$\lambda_k\in\{0,0.1,1,10\}$ in all three modes exchanges no catastrophic
trial and moves inlier RMSE by at most $2$ parts in $10^5$, the attained
residual staying identical to machine precision. That invariance is what the rank accounting of
Section~\ref{subsec:ccop} leads one to expect: in the reference geometry the scalar mask
drops $\rank\bG_0$ from $2K$ to $K$ and the dual mask leaves
$\cond\bG_0\sim10^3$ against $1.1$ at full EVS, and $\bR_{\rm dir}$ acts
only in the directions those deficiencies make invisible.

\begin{figure*}[!tb]
\centering
\includegraphics[width=0.80\textwidth]{fig_resolvability.pdf}
\caption{Polarimetric panel separability at $-10$~dB, the mechanism of
Theorem~\ref{thm:split}(ii): resolution probability -- the fraction of
trials returning $K$ correctly paired components with Phase-I delay error at
most $0.5$~ns and no pole collapse -- against cascaded-delay
separation at four Jones overlaps with exact Clopper--Pearson bands, for
(a)~scalar, (b)~dual-polarized and (c)~full-EVS reception. The dashed
vertical line marks the reference scene's minimum separation and the boxed
number the largest spread over overlap.}
\label{fig:c1_separability}
\end{figure*}

\subsection{C2: Sufficient Compression and Reliable Acquisition}
\label{subsec:res_c2}

\textit{Effect of sufficient compression on acquisition reliability.}
Theorem~\ref{thm:compress} establishes that the projection preserves the
profiled maximizer and the free-Jones information, so what the experiment
asks is what an exactly sufficient projection does to the finite-sample
ESPRIT front end; from $0$~dB upward the compressed and raw routes are
numerically coincident, that finite-sample advantage having saturated. At
$-10$~dB the leakage
ratio $\sigma_{K+1}/\sigma_K$ falls from $0.822$ to $0.353$, the delay p95
error improves $35\times$ ($4.85\to0.140$~ns), basin acquisition rises from
$97.50\%$ to $99.38\%$ and the catastrophic position rate falls from
$2.29\%$ to $0.62\%$. Exact-model accuracy is therefore
unaltered; what the identity permits is discarding $90$ of $96$ receive
dimensions before a subspace method is applied to the remainder.

\textit{Contribution of the individual Phase~I mechanisms.} Table~\ref{tab:c2_ladder}
separates the three Phase~I mechanisms on a nested ladder at $-20$~dB, a
stress point below the declared operating region chosen because the tail
there is large enough to resolve them. Compression alone removes three
quarters of the
catastrophic mass ($21.46\to5.62\%$) by denoising the delay poles;
common-geometry fusion, by turning $K$ disagreeing panel-local fixes into
one position, is what converts that into basin capture
($78.12\to96.04\%$); the additional deterministic starts recover only two
further trials; and the anchor refresh leaves both the rate and the basin
untouched, its evidence being the p95 reconstruction NMSE, which it lowers
$27$-fold from $2.6\times10^{-2}$ to $9.6\times10^{-4}$. A seven-start pool
lands within one trial of the proposed rung, so the gain is not restart
diversity.

\textit{Aperture scaling.} Growing the panel
from $16^2$ to $64^2$ elements lowers conditional RMSE from $1.66$ to
$0.42$~mm while holding the catastrophic rate in $[0.21,0.62]\%$, so the
estimator realizes the aperture gain that motivates near-field operation.
Collecting it is an acquisition property, not a refinement property: the
proposed Phase~I holds a basin rate of $99.2$--$99.6\%$ across that
sixteenfold change in element count, whereas the baseline Phase~I ranges
from $14.58\%$ at $M_R=16^2$ to $95.62\%$ at $64^2$ and never reaches the
proposed rate at any tested aperture. Accuracy improves monotonically with
$M_A$, $T$ and bandwidth, and the ordering persists at higher panel count:
at $K=4$ the proposed route and 4D-JVP carry catastrophic rates of $1.88\%$
and $12.92\%$.

\begin{table}[!t]
\caption{Phase-I mechanism ladder at $-20$~dB over $480$ paired trials. Each
rung adds one mechanism ahead of the same Phase~II route; the bold rung is
the proposed Phase~I, and ``p95 NMSE'' the channel reconstruction error.
The operating-region block is supplementary.}
\label{tab:c2_ladder}
\centering
\scriptsize
\setlength{\tabcolsep}{2.0pt}
\begin{tabular}{lcccc}
\toprule
Phase~I route & $P_{\rm cat}$ [\%] & Basin [\%] & p95 [m] & p95 NMSE\\
\midrule
Baseline (raw EVS) & $21.46\ [17.9,25.4]$ & $62.08$ & $1.618$ &
$9.9\!\times\!10^{-1}$\\
\;+ MKSC compression & $5.62\ [3.7,8.1]$ & $78.12$ & $0.276$ &
$8.8\!\times\!10^{-1}$\\
\;+ geometry ($1$ start) & $3.96\ [2.4,6.1]$ & $96.04$ &
$3.09\!\times\!10^{-3}$ & $2.7\!\times\!10^{-2}$\\
\;+ $4$ det.\ starts & $3.54\ [2.1,5.6]$ & $96.25$ &
$3.05\!\times\!10^{-3}$ & $2.6\!\times\!10^{-2}$\\
\;+ anchor refresh & $\mathbf{3.54}\ [2.1,5.6]$ &
$\mathbf{96.25}$ & $\mathbf{3.06\!\times\!10^{-3}}$ &
$\mathbf{9.6\!\times\!10^{-4}}$\\
\;\;\emph{ctrl:} oracle & $3.33\ [1.9,5.4]$ & $96.46$ &
$2.98\!\times\!10^{-3}$ & $9.4\!\times\!10^{-4}$\\
\bottomrule
\end{tabular}
\end{table}

\subsection{C3: Clock Profiling and Certification}
\label{subsec:res_c3}

Table~\ref{tab:c3_control} isolates Phase~II at $-10$~dB by giving four
configurations the same $100$ MKSC-GI initializers. CCOP-JVP matches the precision
of 4D-JVP while removing one nonlinear coordinate and cutting the median
outer evaluation count from $13$ to $9$, and it eliminates sensitivity to
the clock coordinate's units: the explicit search depends on whether the
clock is carried in seconds, nanoseconds or range units --
the seconds label degrading conditional position RMSE by $31\%$ -- whereas
CCOP-JVP returns bit-identical output under all three parameterizations, as
Theorem~\ref{thm:clock}(ii) requires.

The certification condition is satisfied throughout the tested operating
region: in the independently seeded internal suite, every one of the
$35\,833$ clock profiles solved across its $4\,320$ operating-region trials
returned a gap below its tolerance, the worst reaching $0.99999$ of that
tolerance, so the subdivision cap was never reached.
The certificate covers the inner solve only, and indeed 4D-JVP and CCOP-JVP
sharing one Phase~I
initializer have identical catastrophic rates at every SNR of the
independent suite, which is why the tail suppression of
Section~\ref{subsec:res_c2} is attributed to C2 and not to C3.

\begin{table}[!t]
\caption{Phase-II refinement control at $-10$~dB over $100$ shared MKSC-GI
initializers. The three 4D-JVP rows differ only in clock parameterization.
``Inv.'' is unit invariance, ``Cert.'' a certified inner clock solve, and
RMSEs are conditional on each row's inliers.}
\label{tab:c3_control}
\centering
\scriptsize
\setlength{\tabcolsep}{2.6pt}
\begin{tabular}{lcccccc}
\toprule
Phase~II route & Pos.\ [mm] & Clock [ps] & $P_{\rm cat}$ [\%] & Med.\ eval. &
Inv. & Cert.\\
\midrule
4D-JVP, seconds & $0.6643$ & $3.6780$ & $1$ & $13$ & no & no\\
4D-JVP, ns & $0.5092$ & $3.4607$ & $1$ & $17$ & no & no\\
4D-JVP, $c_0\Delta t$ & $0.5088$ & $3.4438$ & $0$ & $13$ & no & no\\
\textbf{CCOP-JVP} & $\mathbf{0.5088}$ & $\mathbf{3.4438}$ &
$\mathbf{0}$ & $\mathbf{9}$ & yes & yes\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[!tb]
\centering
\includegraphics[width=\textwidth]{fig_boundaries.pdf}
\caption{Operating boundaries at $-10$~dB
(Section~\ref{subsec:res_robustness}). (a)--(b)~RIS--BS angular-calibration
error: accuracy floor and capture boundary. (c)~Per-position catastrophic
rate against the minimum true cascaded-delay separation over the
$5\times5\times2$ grid, marker color being the median free-Jones EFIM
condition number; the shaded strip marks the coincidence region and the
dash-dotted line the reference scene.}
\label{fig:boundaries}
\end{figure*}

\subsection{Deployment Boundaries and Spatial Generalization}
\label{subsec:res_robustness}

Three sensitivities set the deployment envelope, two of them in
Fig.~\ref{fig:boundaries}; the model-order panel, the remaining mismatch and
generalization sweeps and the audit of the frozen Phase~I constants are
supplementary.
\textit{Calibration} is the binding one
(Fig.~\ref{fig:boundaries}(a)--(b)): RIS--BS angular error degrades
precision long before capture,
raising conditional RMSE from $0.447$ to $31.2$~mm at $0.25^\circ$ while
the basin rate holds at $99.17\%$, and capture itself collapses only at
$1^\circ$ ($59.58\%$). Receiver gain, phase and sensor-position mismatch,
RIS element-phase error and first-order colored noise are all milder over
their tested ranges.

\textit{Model order} is a material assumption rather than a tuning
parameter. In the dedicated $0$-dB sweep, $\widehat K=2$ is structurally
destructive ($0.710$-m median, $P_{\rm cat}=65.4\%$), and $\widehat K=4$
and $5$ keep submillimeter medians while carrying $35.4\%$ and $49.0\%$
catastrophic rates.

\textit{Delay coincidence} bounds acquisition where information does not
(Fig.~\ref{fig:boundaries}(c)). Across a $5\times5\times2$ position grid the proposed median per-position
catastrophic rate is $0.62\%$ against $2.92\%$ for 4D-JVP, and both hard
corners lie near a cascaded-delay coincidence surface, the worst reaching
$31.25\%$ at a minimum separation of $0.062/B_{\rm occ}$ -- while the
free-Jones EFIM stays full rank at every tested position, corners included.
Local information sufficiency therefore does not imply global acquisition
reliability: deployment screening needs a delay-coincidence check and a
coarse global fallback, not a PEB map alone. Every sweep here holds one
infrastructure layout and samples no panel-placement diversity.

\subsection{Cost, Training Budget, and Failure Diagnostics}
\label{subsec:res_cost}

\textit{Cost.} In the serialized single-thread benchmark at $-10$~dB the
proposed route requires $3.20$~s and $2.52$~GB peak resident set size on
the $96$ calibrated receive chains a full EVS costs:
faster than Algebraic CPD ($4.51$~s) and near-field OMP--SAGE--WLS
($11.5$~s), slower than 4D-JVP ($2.42$~s) and VBI/SBL's native
read-out ($1.06$~s), the fastest of the routes but the one that pays the
floor of Section~\ref{subsec:res_benchmark}; all timings share one host, absolute
seconds not being portable. An independently seeded $30$-trial
decomposition agrees with that total to under $1\%$ and places $2.86$~s of
it in Phase~I against $0.33$~s in certified clock profiling, so acquisition
and not refinement dominates the budget. The per-block breakdown and
per-method memory are supplementary.

\textit{Training and mobility.} A $25\%$ cyclic prefix at $\Delta f=5$~MHz
is $50$~ns, covering the $19.1$-ns sum of cascaded-delay spread and
clock-search interval, so one configuration per symbol gives
$T_{\rm train}\simeq64~\mu$s at $T=256$, excluding reconfiguration latency.
At $1$~m/s the UE then moves $64~\mu$m per sweep, $16\%$ of the median PEB,
the two becoming equal near $6$~m/s: a displacement check against a static
bound, not a time-varying channel experiment.

\textit{Failure localization.} Across the $4\,320$ operating-region trials,
exactly five proposed-route trials are catastrophic, and exactly those five
fail the basin diagnostic, each with a Phase~I delay RMSE above $0.5$~ns;
panel association is correct in $99.93\%$ of trials overall and in every
trial at and above $0$~dB. Every failure in the tested region therefore
originates in acquisition rather than in refinement.
\section{Conclusion}
\label{sec:conclusion}

We studied joint 3-D localization and synchronization in a blocked-LoS
distributed near-field RIS uplink observed by one shared EVS array, and
showed that a single algebraic fact organizes it: the per-panel sensing block
is a Kronecker product of a space--frequency factor and a calibrated
Maxwell--Jones factor, one consequence for each difficulty the architecture
creates. Its projector splits, so polarization enters the
information as a scalar weight while panel separability obeys a separate
condition a reduced receiver can void outright (C1). Its calibrated factor
confines every response to a $2K$-dimensional receive subspace, making the
$96\to6$ projection lossless for the profiled maximizer and the
free-Jones information (C2). Its clock dependence is one unitary orbit,
turning the conditional profile into a finite trigonometric polynomial solved
to a deterministic optimality-gap certificate and removing a dimension from
the outer search (C3).

Empirically the gain is concentrated in basin reliability rather than in
local precision. The proposed route and 4D-JVP agree to within $1\%$ once they
reach the same basin; what separates them is the catastrophic tail, and every
catastrophic trial in the operating-region suite fails the Phase-I basin
diagnostic. The tail is therefore a property of acquisition -- EVS
discrimination, compressed delay recovery, panel association and
common-geometry fusion -- not of refinement: sharing one initializer, the two
refinement routes return identical catastrophic rates across the independent
suite. CCOP-JVP contributes a lower-dimensional, unit-invariant refinement
with a certified inner solve, not a global outer search.

Local information is not what fails first here: retaining fewer Maxwell
components rescales a panel's information by a known scalar yet can drive to
zero the residual separating one panel from another, and the free-Jones EFIM
stays full rank even at the swept positions whose catastrophic rate reaches
$31\%$ near a cascaded-delay coincidence surface. What degrades first is
discriminability -- of panels from one another, and of the correct basin from
its competitors -- which a position-error bound cannot see. Receiver-mode
selection and deployment screening should therefore rest on the
Maxwell-subspace principal angle and the cascaded-delay separation, not on a
bound map alone.

Three boundaries remain within the model: RIS--BS angular calibration limits
millimeter accuracy well before it limits capture; the panel count is a
material model-order assumption; and the factorial first tier of panel
association, negligible at $K=3$, binds as panels are added. Spatial-wideband
effects, direct leakage and diffuse multipath, and panel-placement diversity
lie outside it. The two nontrivial openings are a polynomial-time association
surrogate and coarse global screening of coincidence regions.

\balance
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
